var documenterSearchIndex = {"docs":
[{"category":"section","location":"#GenomicBreedingDB","page":"Home","text":"Documentation for GenomicBreedingDB.\n\n","title":"GenomicBreedingDB"},{"category":"type","location":"#GenomicBreedingDB.Filter","page":"Home","text":"Filter(\n    conn::LibPQ.Connection;\n    table::String,\n    field::String,\n    filter_like::Union{Nothing,String}=nothing,\n    filter_in::Union{\n        Nothing,\n        Vector{String},\n        Vector{Int},\n        Vector{AbstractFloat}\n    }=nothing,\n    filter_between::Union{\n        Nothing,\n        Tuple{Int,Int},\n        Tuple{AbstractFloat,AbstractFloat}\n    }=nothing,\n    filter_equal_to::Union{Nothing,Int,AbstractFloat}=nothing,\n    filter_less_than::Union{Nothing,Int,AbstractFloat}=nothing,\n    filter_greater_than::Union{Nothing,Int,AbstractFloat}=nothing,\n)\n\nConstruct a validated database filter for querying, updating, or deleting records.\n\nA Filter encapsulates a single filtering criterion applied to a database table. The constructor validates the target table and field, ensures that exactly one filter condition has been supplied, and automatically resolves human-readable entity names into database identifiers when filtering on foreign-key fields.\n\nIf a supplied field does not exist in the target table, the constructor attempts to infer a related foreign-key field. For example, fields such as entry, entries, species, site, or trait may be mapped automatically to their corresponding identifier fields (entry_id, species_id, site_id, trait_id, etc.) when those fields exist in the database schema.\n\nWhen filtering on identifier fields, supplied names are automatically translated into their corresponding database ids using extract_ids. This allows filters to be expressed using meaningful biological or experimental identifiers rather than internal database keys.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ntable::String: Name of the table to filter.\nfield::String: Name of the field on which filtering will be applied.\nfilter_like::Union{Nothing,String}=nothing: Pattern-matching filter using SQL ILIKE semantics.\nfilter_in::Union{Nothing,Vector{String},Vector{Int},Vector{AbstractFloat}}=nothing: Filter matching any value in the supplied collection.\nfilter_between::Union{Nothing,Tuple{Int,Int},Tuple{AbstractFloat,AbstractFloat}}=nothing: Inclusive range filter.\nfilter_equal_to::Union{Nothing,Int,AbstractFloat}=nothing: Exact numeric equality filter.\nfilter_less_than::Union{Nothing,Int,AbstractFloat}=nothing: Numeric less-than filter.\nfilter_greater_than::Union{Nothing,Int,AbstractFloat}=nothing: Numeric greater-than filter.\n\nFields\n\ntable::String: Target database table.\nfield::String: Database field used in the filter expression.\nlike::Union{Nothing,String}: Pattern-matching filter value.\nin::Union{Nothing,Vector{String},Vector{Int},Vector{AbstractFloat}}: Collection-based filter values.\nbetween::Union{Nothing,Tuple{Int,Int},Tuple{AbstractFloat,AbstractFloat}}: Inclusive range filter values.\nequal_to::Union{Nothing,Int,AbstractFloat}: Equality filter value.\nless_than::Union{Nothing,Int,AbstractFloat}: Less-than filter value.\ngreater_than::Union{Nothing,Int,AbstractFloat}: Greater-than filter value.\n\nThrows\n\nErrorException: If the target table does not exist.\nErrorException: If the specified field cannot be resolved to a valid field.\nErrorException: If zero or multiple filter criteria are supplied.\nErrorException: If supplied names cannot be resolved to database ids.\nErrorException: If no database records match a relational filter value.\nAny exception raised during database validation or identifier resolution.\n\nNotes\n\nExactly one filtering criterion must be supplied.\nConnection and schema validation are performed using check.\nIf the supplied field is not present in the table, the constructor attempts to infer a suitable foreign-key field by appending _id.\nSpecial handling is provided for:\nentries → entry_id\nspecies → species_id\nName-based filters applied to foreign-key fields are automatically translated into numeric identifiers using extract_ids.\nForeign-key mappings are resolved through the corresponding lookup tables:\nentry_id ↔ entries\nspecies_id ↔ species\n<name>_id ↔ <name>s\nfilter_like searches on foreign-key fields are resolved to matching ids and subsequently converted into IN filters.\nPattern-matching filters automatically receive surrounding % wildcards when not already supplied.\nUnderscore characters are escaped to prevent unintended SQL wildcard matching.\nThe resulting Filter object contains fully resolved values and is ready for use by functions such as query_table, concat_filters, update_table!, and delete_names!.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> try Filter(conn, table=\"entries\", field=\"name\", filter_like=\"_010\", filter_in=\"entry_100\"); catch; false; end\nfalse\n\njulia> Filter(conn, table=\"entries\", field=\"name\", filter_like=\"010\")\nFilter(\"entries\", \"name\", \"%010%\", nothing, nothing, nothing, nothing, nothing)\n\njulia> x = Filter(conn, table=\"phenotype_data\", field=\"entry\", filter_like=\"entry_100\");\n\njulia> (x.table == \"phenotype_data\") && (x.field == \"entry_id\") && isnothing(x.like) && !isnothing(x.in)\ntrue\n\njulia> x = Filter(conn, table=\"phenotype_data\", field=\"site\", filter_in=[\"site_1\", \"site_2\"]);\n\njulia> (x.table == \"phenotype_data\") && (x.field == \"site_id\") && isnothing(x.like) && !isnothing(x.in)\ntrue\n\njulia> x = Filter(conn, table=\"phenotype_data\", field=\"value\", filter_between=(10, 20));\n\njulia> (x.table == \"phenotype_data\") && (x.field == \"value\") && (x.between == (10, 20))\ntrue\n\njulia> x = Filter(conn, table=\"phenotype_data\", field=\"value\", filter_equal_to=10);\n\njulia> (x.table == \"phenotype_data\") && (x.field == \"value\") && (x.equal_to == 10)\ntrue\n\njulia> x = Filter(conn, table=\"phenotype_data\", field=\"value\", filter_less_than=10);\n\njulia> (x.table == \"phenotype_data\") && (x.field == \"value\") && (x.less_than == 10)\ntrue\n\njulia> x = Filter(conn, table=\"phenotype_data\", field=\"value\", filter_greater_than=10);\n\njulia> (x.table == \"phenotype_data\") && (x.field == \"value\") && (x.greater_than == 10)\ntrue\n\njulia> x = Filter(conn, table=\"entries\", field=\"name\", filter_like=\"entry_100\");\n\njulia> !isnothing(match(Regex(\"^%\"), x.like))\ntrue\n\njulia> !isnothing(match(Regex(\"_\"), x.like))\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.Filter"},{"category":"method","location":"#Base.:(==)-Tuple{Filter, Filter}","page":"Home","text":"Base.:(==)(\n    x::Filter,\n    y::Filter,\n)::Bool\n\nDetermine whether two Filter objects are equal.\n\nTwo Filter objects are considered equal when all corresponding fields contain identical values. Equality is evaluated by comparing each field in the order defined by the Filter structure, and comparison terminates immediately when a difference is detected.\n\nThis method extends Julia's standard equality operator for the Filter type and provides behaviour consistent with the custom hash implementation. The primary use of this method is so that the unique function on a vector of Filter objects work as intended.\n\nArguments\n\nx::Filter: First filter object.\ny::Filter: Second filter object.\n\nReturns\n\nBool: true if all fields of x and y are equal; otherwise false.\n\nNotes\n\nEquality is determined by comparing every field in the Filter structure.\nComparison is performed using the != operator on each corresponding field.\nEvaluation stops as soon as a mismatch is detected.\nFilters that contain identical values in all fields are considered equal.\nThis implementation is intended to remain consistent with Base.hash(::Filter, ::UInt), enabling reliable use of Filter objects in hash-based collections such as Set and Dict.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> table = \"phenotype_data\";\n\njulia> filters = Filter[];\n\njulia> push!(filters, Filter(conn, table=table, field=\"entry\", filter_like=\"1\"));\n\njulia> push!(filters, Filter(conn, table=table, field=\"entry\", filter_like=\"1\"));\n\njulia> push!(filters, Filter(conn, table=table, field=\"site\", filter_in=[\"site_1\", \"site_2\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"value\", filter_between=(10, 20)));\n\njulia> filters[1] == filters[2]\ntrue\n\njulia> filters[1] != filters[3]\ntrue\n\njulia> length(filters) > length(unique(filters))\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"Base.:(==)"},{"category":"method","location":"#Base.hash-Tuple{Filter, UInt64}","page":"Home","text":"Base.hash(\n    x::Filter,\n    h::UInt,\n)::UInt\n\nCompute a hash value for a Filter object.\n\nThe method extends Julia's hashing interface for the Filter type by combining the hash values of all fields contained within the object. The resulting hash depends on the values of every field, ensuring that filters with identical contents produce identical hash values.\n\nThis implementation enables Filter objects to be used reliably in hashed data structures such as Dict, Set, and other collections that depend on hash-based lookup.\n\nArguments\n\nx::Filter: Filter object to hash.\nh::UInt: Initial hash seed.\n\nReturns\n\nUInt: Hash value representing the contents of the Filter object.\n\nNotes\n\nHash values are generated by iteratively combining the hashes of all fields in the Filter object.\nField values are processed in the order returned by fieldnames(typeof(x)).\nThe implementation is compatible with Julia's standard hashing framework.\nObjects containing identical field values will produce identical hash values when supplied with the same hash seed.\nThis method should remain consistent with any corresponding isequal implementation for the Filter type.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> f = Filter(conn, table=\"entries\", field=\"name\", filter_like=\"_010\");\n\njulia> isa(hash(f), UInt)\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"Base.hash"},{"category":"method","location":"#GenomicBreedingDB.add_col!-Tuple{DataFrames.DataFrame}","page":"Home","text":"add_col!(\n    df::DataFrame;\n    col::String,\n    value::Union{Nothing,String},\n)::Nothing\n\nAdd a constant-valued column to a DataFrame if the column does not already exist.\n\nIf col is already present in df, the function leaves the existing column unchanged. When a non-nothing value is supplied in this situation, a warning is emitted indicating that the existing column will be used instead of the provided value.\n\nIf the column does not exist, a value must be supplied. The column name and value are validated for illegal strings before a new column is created and populated with the specified value for all rows.\n\nArguments\n\ndf::DataFrame: DataFrame to modify.\ncol::String: Name of the column to add.\nvalue::Union{Nothing,String}: Value to assign to all rows of the new column. Must not be nothing when the column does not already exist.\n\nReturns\n\nNothing: The input DataFrame is modified in place.\n\nThrows\n\nErrorException: If the column does not exist and value is nothing.\nErrorException: If the column name or value contains illegal strings.\n\nWarnings\n\nA warning is emitted when col already exists and a non-nothing value is supplied, as the provided value is ignored.\n\nNotes\n\nThe function performs in-place modification and does not return the modified DataFrame.\nExisting columns are never overwritten.\nBoth the proposed column name and value are validated using check_illegal_strings before insertion.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> p = ncol(df);\n\njulia> add_col!(df, col=\"some_new_column\", value=\"Dolorem ipsum\")\n\njulia> p < ncol(df)\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.add_col!"},{"category":"method","location":"#GenomicBreedingDB.add_measurement_dates!-Tuple{DataFrames.DataFrame}","page":"Home","text":"add_measurement_dates!(\n    df::DataFrame;\n    measurement_dates::Union{Nothing,Dict{String,String}}=nothing,\n)::Nothing\n\nAdd or validate a dates column for measurement records in a trial DataFrame.\n\nIf a dates column already exists, the function validates that all date values are in a recognised date format. When measurement_dates is supplied in this case, the existing dates column takes precedence and a warning is emitted.\n\nIf no dates column exists, dates are derived from the measurements column. When measurement_dates is not provided, measurement names are assumed to represent dates directly and are parsed into Date values. When measurement_dates is supplied, it is used to map measurement identifiers to their corresponding dates.\n\nA new dates column is added to the input DataFrame when required.\n\nArguments\n\ndf::DataFrame: DataFrame containing measurement records.\nmeasurement_dates::Union{Nothing,Dict{String,String}}=nothing: Optional mapping between measurement identifiers and date strings in yyyy-mm-dd format.\n\nReturns\n\nNothing: The input DataFrame is modified in place.\n\nThrows\n\nErrorException: If neither a dates column nor a measurements column is available.\nErrorException: If one or more date values are not in the required yyyy-mm-dd format.\nErrorException: If a measurement defined in measurement_dates cannot be found in the DataFrame.\nErrorException: If dates are not supplied for all measurements present in the DataFrame when using measurement_dates.\n\nWarnings\n\nA warning is emitted when both a dates column and measurement_dates are provided, as the existing dates column is used.\n\nNotes\n\nDate strings are expected to follow the yyyy-mm-dd format.\nWhen a dates column is already present, only validation is performed.\nNon-string measurement identifiers are converted to String before processing.\nAll unique measurements must have a corresponding entry in measurement_dates when a mapping is supplied.\nThe function performs all modifications in place.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\"); \n\njulia> measurements = String.(unique(df.measurements));\n\njulia> measurement_dates::Dict{String,String} = Dict(); [measurement_dates[x] = x for x in measurements];\n\njulia> df_1 = deepcopy(df); df_2 = deepcopy(df);\n\njulia> add_measurement_dates!(df_1);\n\njulia> add_measurement_dates!(df_2, measurement_dates=measurement_dates);\n\njulia> isa(df_1.dates, Vector{DateTime})\ntrue\n\njulia> df_1.dates == df_2.dates\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.add_measurement_dates!"},{"category":"method","location":"#GenomicBreedingDB.check-Tuple{DataFrames.DataFrame, String}","page":"Home","text":"check(\n    df::DataFrame,\n    col::String,\n)::Nothing\n\nValidate that a DataFrame contains a specified column and, when applicable, validate the string values within that column.\n\nThe function verifies that col exists in the supplied DataFrame. If the column contains string values, all unique entries are validated using check_illegal_strings to ensure they conform to the project's naming and identifier conventions.\n\nIf validation fails, the underlying error is rethrown with additional context identifying the offending column.\n\nArguments\n\ndf::DataFrame: DataFrame to validate.\ncol::String: Name of the column that must exist and, if applicable, contain valid string values.\n\nReturns\n\nNothing: Returned when the column exists and all validation checks pass.\n\nThrows\n\nErrorException: If the specified column does not exist in the DataFrame.\nErrorException: If the column contains invalid string values.\nAny exception raised by check_illegal_strings, wrapped with contextual information identifying the column being validated.\n\nNotes\n\nString validation is performed only when the column element type is a subtype of AbstractString.\nUnique string values are validated to avoid redundant checks.\nValidation of string content is delegated to check_illegal_strings.\nThe function does not modify the input DataFrame.\n\nExamples\n\njulia> df = DataFrame(aye=[\"abc\", \"def\", \"ghi\"], nay=[\"hello world ^_^\", \"wildcard*\", \"slash/slash\"]);\n\njulia> try isnothing(check(df, \"aye\")); catch; false; end\ntrue\n\njulia> try isnothing(check(df, \"nay\")); catch; false; end\nfalse\n\n\n\n\n\n","title":"GenomicBreedingDB.check"},{"category":"method","location":"#GenomicBreedingDB.check-Tuple{LibPQ.Connection, String, String}","page":"Home","text":"check(\n    conn::LibPQ.Connection,\n    table::String,\n    field::String,\n)::Nothing\n\nValidate that a database connection is open and that a specified field exists within a database table.\n\nThe function first verifies that the supplied PostgreSQL connection is active using check(conn). It then validates both the table and field names against the project's naming conventions before confirming that the specified field exists in the target table and has not been dropped.\n\nField existence is determined using PostgreSQL system catalogue metadata stored in pg_attribute.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ntable::String: Name of the table containing the field.\nfield::String: Name of the field to validate.\n\nReturns\n\nNothing: Returned when the connection is open and the specified field exists in the target table.\n\nThrows\n\nErrorException: If the database connection has been closed.\nErrorException: If the table name contains illegal characters or strings.\nErrorException: If the field name contains illegal characters or strings.\nErrorException: If the specified field does not exist in the target table.\nAny database exception raised whilst checking field existence.\n\nNotes\n\nConnection validation is delegated to check(conn).\nTable and field names are validated using check_illegal_strings.\nField existence is determined using PostgreSQL system catalogue information in pg_attribute.\nDropped fields are excluded from the existence check using NOT attisdropped.\nThe target table is resolved using PostgreSQL's regclass mechanism.\nThe function performs validation only and does not modify the database.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> try isnothing(check(conn, \"entries\", \"name\")); catch; false; end\ntrue\n\njulia> try isnothing(check(conn, \"entries\", \"this_field_does_not_exist\")); catch; false; end\nfalse\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.check"},{"category":"method","location":"#GenomicBreedingDB.check-Tuple{LibPQ.Connection, String}","page":"Home","text":"check(\n    conn::LibPQ.Connection,\n    table::String,\n)::Nothing\n\nValidate that a database connection is open and that a specified table exists in the database.\n\nThe function first verifies that the supplied PostgreSQL connection is active using check(conn). It then validates the table name against the project's naming conventions and confirms that the table exists within the public schema of the connected database.\n\nTable existence is determined using PostgreSQL's to_regclass function, which provides a reliable mechanism for identifying registered database objects.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ntable::String: Name of the table to validate.\n\nReturns\n\nNothing: Returned when the connection is open and the table exists.\n\nThrows\n\nErrorException: If the database connection has been closed.\nErrorException: If the table name contains illegal characters or strings.\nErrorException: If the specified table does not exist in the database.\nAny database exception raised while checking table existence.\n\nNotes\n\nConnection validation is delegated to check(conn).\nTable-name validation is performed using check_illegal_strings.\nExistence checks are performed against the public schema.\nPostgreSQL's to_regclass function is used to determine whether the table is present in the database.\nThe function performs validation only and does not modify the database.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> try isnothing(check(conn, \"entries\")); catch; false; end\ntrue\n\njulia> try isnothing(check(conn, \"this_table_does_not_exist\")); catch; false; end\nfalse\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.check"},{"category":"method","location":"#GenomicBreedingDB.check-Tuple{LibPQ.Connection}","page":"Home","text":"check(\n    conn::LibPQ.Connection,\n)::Nothing\n\nValidate that a database connection is open and available for use.\n\nThe function checks whether the supplied PostgreSQL connection has been closed. If the connection is no longer active, an error is raised instructing the user to establish a new connection before continuing.\n\nThis validation function is intended to be used defensively before performing database operations that require an active connection.\n\nArguments\n\nconn::LibPQ.Connection: PostgreSQL database connection to validate.\n\nReturns\n\nNothing: Returned when the connection is open and usable.\n\nThrows\n\nErrorException: If the database connection has been closed.\n\nNotes\n\nThe connection state is determined using the closed property of the LibPQ.Connection object.\nThe function performs validation only and does not modify the connection.\nThis check can be used before executing queries, updates, inserts, or other database operations that require an active session.\nA closed connection cannot be reopened and must be replaced with a new connection created via dbconnect().\n\nExamples\n\n```jldoctest; setup=:(using GenomicBreedingCore, GenomicBreedingIO, GenomicBreedingDB, DataFrames, CSV, StatsBase, LibPQ, Dates) julia> conn = dbconnect();\n\njulia> try isnothing(check(conn)); catch; false; end true\n\njulia> close(conn);\n\njulia> try isnothing(check(conn)); catch; false; end false ````\n\n\n\n\n\n","title":"GenomicBreedingDB.check"},{"category":"method","location":"#GenomicBreedingDB.check-Union{Tuple{Type{T}}, Tuple{T}} where T<:AbstractGB","page":"Home","text":"check(\n    type::Type{T};\n    fname::String,\n)::Nothing where {T<:AbstractGB}\n\nValidate that a file exists and appears to be a JLD2 file containing an object of the specified GenomicBreeding type.\n\nThe function first verifies that the supplied file exists and then performs a lightweight validation by inspecting the beginning of the file for signatures indicating that it is a Julia-generated HDF5-backed JLD2 file containing an object whose type matches the supplied AbstractGB subtype.\n\nValidation succeeds only when the file contents contain the strings Julia, HDF5, and the name of the requested type. If any of these signatures are absent, an error is raised.\n\nType Parameters\n\nT <: AbstractGB: Expected GenomicBreeding object type stored in the JLD2 file, such as Genomes, Phenomes, or Fit.\n\nArguments\n\ntype::Type{T}: Expected object type contained within the file.\nfname::String: Path to the JLD2 file to validate.\n\nReturns\n\nNothing: Returned when the file exists and appears to contain an object of the specified type.\n\nThrows\n\nErrorException: If the specified file does not exist.\nErrorException: If the file does not appear to be a JLD2 file containing an object of the specified type.\nAny exception raised while opening or reading the file.\n\nNotes\n\nFile existence is verified before any content validation is performed.\nValidation is performed by reading the first 1,000 bytes of the file.\nThe function checks for the presence of the strings:\nJulia\nHDF5\nstring(type)\nThis is a lightweight heuristic validation and does not fully deserialize the object.\nThe function is intended to quickly verify file compatibility before loading larger datasets.\nTypical supported types include Genomes, Phenomes, Fit, and other subtypes of AbstractGB.\n\nExamples\n\njulia> genomes = simulate_genomes();\n\njulia> phenomes = simulate_trials(genomes) |> simulate_phenomes;\n\njulia> simulate_fit(genomes, phenomes);\n\njulia> try isnothing(check(Genomes, fname=\"simulated_genomes.jld2\")); catch; false; end\ntrue\n\njulia> try isnothing(check(Phenomes, fname=\"simulated_phenomes.jld2\")); catch; false; end\ntrue\n\njulia> try isnothing(check(Fit, fname=\"simulated_fit.jld2\")); catch; false; end\ntrue\n\njulia> try isnothing(check(Fit, fname=\"some_non_exitent_file.jld2\")); catch; false; end\nfalse\n\n\n\n\n\n","title":"GenomicBreedingDB.check"},{"category":"method","location":"#GenomicBreedingDB.check_illegal_strings-Tuple{Vector{String}}","page":"Home","text":"check_illegal_strings(\n    x::Vector{String};\n    additional_illegal_strings::Union{Nothing,Vector{String}}=nothing,\n)::Nothing\n\nValidate a collection of strings against a predefined set of prohibited characters and patterns.\n\nThe function enforces a strict naming convention intended for identifiers and metadata stored in the database. Each string is checked for non-ASCII characters, a predefined set of illegal characters, and optionally a collection of user-specified disallowed string patterns.\n\nIf one or more violations are detected, an informative error is raised describing all invalid strings and the corresponding offending characters or patterns.\n\nArguments\n\nx::Vector{String}: Collection of strings to validate.\nadditional_illegal_strings::Union{Nothing,Vector{String}}=nothing: Optional list of additional string patterns that are not permitted.\n\nReturns\n\nNothing: Returned when all supplied strings pass validation.\n\nThrows\n\nErrorException: If x is empty.\nErrorException: If one or more strings contain non-ASCII characters.\nErrorException: If one or more strings contain prohibited characters.\nErrorException: If one or more strings match a pattern listed in additional_illegal_strings.\n\nNotes\n\nValidation is intentionally restrictive to promote consistent naming and identifier conventions throughout the database.\nNon-ASCII characters are not permitted.\nProhibited characters include punctuation and symbols such as: ;, |, ,, ., /, \\, \", ', , ~, !, @, #, $, %, ^, &, *, (, ), +, =, {, }, [, ], :, <, >, and ?.\nAdditional prohibited patterns may be supplied using additional_illegal_strings.\nAll detected validation errors are reported together whenever possible.\n\nExamples\n\njulia> x_legals = String[\"geno_1\", \"2026-07-08\", \"ABC-def_123-2026\", \"camelCase\"];\n\njulia> x_illegals = String[\"geno.1\", \"2026/07/08\", \"ABC|def_123-2026;\", \"#camelCase%\", \"∈LEMENT\"];\n\njulia> isnothing(check_illegal_strings(x_legals))\ntrue\n\njulia> try check_illegal_strings(x_illegals); catch; true; end\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.check_illegal_strings"},{"category":"method","location":"#GenomicBreedingDB.check_reference_genome-Tuple{String}","page":"Home","text":"check_reference_genome(\n    fname::String,\n)::Nothing\n\nValidate that a reference genome file exists and appears to be a valid FASTA file.\n\nThe function verifies that the supplied file exists and performs a lightweight validation of its contents by locating the first FASTA record and inspecting the associated sequence. Validation succeeds when the sequence contains the canonical DNA nucleotide bases A, T, C, and G.\n\nBoth uncompressed FASTA files and gzip-compressed FASTA files are supported.\n\nArguments\n\nfname::String: Path to the reference genome FASTA file.\n\nReturns\n\nNothing: Returned when the file exists and appears to contain valid FASTA-formatted DNA sequence data.\n\nThrows\n\nErrorException: If the specified file does not exist.\nErrorException: If the file does not appear to be a valid FASTA file.\nAny exception raised whilst opening or reading the file.\n\nNotes\n\nFile existence is verified before content validation is performed.\nBoth plain-text FASTA files and gzip-compressed FASTA files are supported.\nValidation is based on inspection of the first detected FASTA record.\nThe function searches for the first header line beginning with >.\nSequence validation is performed by checking for the presence of the canonical DNA bases A, T, C, and G.\nThis is a lightweight heuristic validation and does not fully parse the FASTA file.\nThe function performs validation only and does not modify the file.\n\nExamples\n\n```jldoctest; setup=:(using GenomicBreedingCore, GenomicBreedingIO, GenomicBreedingDB, DataFrames, CSV, StatsBase, LibPQ, Dates) julia> fnamereferencegenome = string(\"simulatedreferencegenome-\", Dates.now(),\".fa\");\n\njulia> simulategenomes(fnamereferencegenome=fnamereference_genome);\n\njulia> checkreferencegenome(fnamereferencegenome) |> isnothing true ````\n\n\n\n\n\n","title":"GenomicBreedingDB.check_reference_genome"},{"category":"method","location":"#GenomicBreedingDB.check_vcf-Tuple{String}","page":"Home","text":"check_vcf(\n    fname::String,\n)::Nothing\n\nValidate that a file exists and appears to be a valid Variant Call Format (VCF) file.\n\nThe function verifies that the supplied file exists and performs a lightweight validation of its contents by searching for the mandatory VCF header line beginning with #CHROM. Validation succeeds when this header line is detected in either an uncompressed or gzip-compressed file.\n\nBoth plain-text and gzip-compressed VCF files are supported.\n\nArguments\n\nfname::String: Path to the VCF file.\n\nReturns\n\nNothing: Returned when the file exists and appears to contain valid VCF-formatted data.\n\nThrows\n\nErrorException: If the specified file does not exist.\nErrorException: If the file does not appear to be a valid VCF file.\nAny exception raised whilst opening or reading the file.\n\nNotes\n\nFile existence is verified before content validation is performed.\nBoth plain-text and gzip-compressed VCF files are supported.\nValidation is based on detection of the mandatory #CHROM header line.\nThe function scans through comment lines until either #CHROM is found or an invalid record structure is encountered.\nThis is a lightweight heuristic validation and does not verify the complete correctness of the VCF file.\nThe function performs validation only and does not modify the file.\n\nExamples\n\n```jldoctest; setup=:(using GenomicBreedingCore, GenomicBreedingIO, GenomicBreedingDB, DataFrames, CSV, StatsBase, LibPQ, Dates) julia> fnamegenomesvcf = string(\"simulatedgenotypevcf-\", Dates.now(),\".vcf\");\n\njulia> simulategenomes(fnamegenomesvcf=fnamegenomes_vcf);\n\njulia> checkvcf(fnamegenomes_vcf) |> isnothing true ````\n\n\n\n\n\n","title":"GenomicBreedingDB.check_vcf"},{"category":"method","location":"#GenomicBreedingDB.concat_filters-Tuple{Vector{Filter}}","page":"Home","text":"concat_filters(\n    filters::Vector{Filter};\n    verbose::Bool=false,\n)::Tuple{Vector{String},Vector{String}}\n\nConvert a collection of Filter objects into SQL filter clauses and their associated query parameters.\n\nThe function translates validated Filter instances into parameterised SQL fragments suitable for inclusion in a WHERE clause. SQL expressions and query parameters are accumulated in the order supplied, ensuring that parameter indices remain aligned with the generated placeholders.\n\nThe resulting SQL fragments and parameter vector can be used directly in subsequent database queries or update statements.\n\nArguments\n\nfilters::Vector{Filter}: Collection of filters to convert into SQL clauses.\nverbose::Bool=false: If true, display progress information whilst processing the filters.\n\nReturns\n\nTuple{Vector{String},Vector{String}}:\nsql: Vector of SQL filter expressions.\npar: Vector of parameter values corresponding to the generated SQL placeholders.\n\nThrows\n\nErrorException: If a filter does not contain a valid filtering criterion.\n\nNotes\n\nSQL fragments are generated using parameter placeholders rather than embedding values directly into query strings.\nThe function supports the following filter types:\nlike → ILIKE\nin → IN (...)\nbetween → BETWEEN ... AND ...\nequal_to → equality comparison\nless_than → less-than comparison\ngreater_than → greater-than comparison\nParameter numbering is generated dynamically based on the number of previously accumulated parameters.\nAll parameter values are converted to strings before being returned.\nProgress reporting is available when verbose=true.\nThe generated SQL fragments are intended to be concatenated with an existing query rather than executed directly.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> table = \"phenotype_data\";\n\njulia> filters = Filter[];\n\njulia> push!(filters, Filter(conn, table=table, field=\"site\", filter_in=[\"site_1\", \"site_2\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"value\", filter_between=(10, 20)));\n\njulia> filters_cat, par = concat_filters(filters);\n\njulia> (length(filters_cat) == 2) && (length(par) == 4)\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.concat_filters"},{"category":"method","location":"#GenomicBreedingDB.dbconnect-Tuple{}","page":"Home","text":"dbconnect()::LibPQ.Connection\n\nCreate a connection to the configured PostgreSQL database and validate the database schema.\n\nThe function loads database connection settings from a .env file located in the user's home directory and establishes a connection using LibPQ. After connecting, a series of schema validation checks are performed to ensure that required database tables contain the expected name field.\n\nIf the connection cannot be established or the database schema does not satisfy the required conventions, an informative error is raised.\n\nReturns\n\nLibPQ.Connection: Active PostgreSQL database connection.\n\nThrows\n\nErrorException: If the database connection cannot be established.\nErrorException: If required environment variables are missing.\nException: If one or more required database tables do not contain a name field.\nAny exception raised while querying database metadata.\n\nNotes\n\nConnection details are loaded from ~/.env using DotEnv.load!.\nThe following environment variables are required: DB_NAME, DB_USER, DB_PASSWORD, and DB_HOST.\nDatabase connections are established using LibPQ.Connection.\nAfter connecting, the schema is validated using metadata from information_schema.\nThe following tables are expected to contain a mandatory name field: species, entries, experiments, sites, treatments, traits, measurements, reference_genomes, genotype_vcfs, genomes, phenomes, and fits.\nThe function serves as both a connection helper and a schema-validation safeguard for downstream database operations.\n\n\n\n\n\n","title":"GenomicBreedingDB.dbconnect"},{"category":"function","location":"#GenomicBreedingDB.dbinit","page":"Home","text":"dbinit(\n    schema_path::String=\"db/schema.sql\",\n)::Nothing\n\nInitialise the database schema from a SQL definition file.\n\nThe function establishes a database connection, reads the specified schema file, and executes each SQL statement sequentially. Special handling is provided for PostgreSQL function definitions so that multi-statement function bodies are processed correctly despite containing internal semicolons.\n\nErrors encountered during schema creation are collected and reported after all statements have been processed. Certain expected errors related to existing enum types may be tolerated, whilst unexpected errors cause database initialisation to fail.\n\nArguments\n\nschema_path::String=\"db/schema.sql\": Path to the SQL schema file used to initialise the database.\n\nReturns\n\nNothing: Database schema objects are created or updated in place.\n\nThrows\n\nErrorException: If the schema file cannot be read.\nErrorException: If database initialisation encounters unexpected errors.\nAny exception raised while connecting to the database.\nAny exception raised while executing schema statements that prevent successful initialisation.\n\nNotes\n\nA database connection is established using dbconnect.\nSQL statements are executed sequentially from the supplied schema file.\nPostgreSQL function definitions are reconstructed before execution to handle embedded semicolons correctly.\nFailed statements trigger a transaction rollback before processing continues.\nErrors are accumulated and evaluated after all statements have been processed.\nErrors associated with existing entry_type or relationship_type definitions may be treated as non-fatal.\nDatabase connections are closed before the function exits.\nThe function is intended for schema creation and maintenance rather than routine database operations.\n\n\n\n\n\n","title":"GenomicBreedingDB.dbinit"},{"category":"method","location":"#GenomicBreedingDB.define_relationships!-Tuple{LibPQ.Connection}","page":"Home","text":"define_relationships!(\n    conn::LibPQ.Connection;\n    table::String,\n    fname_jld2::String,\n    link_value_parser::Function=x -> String(split(x, '|')[1]),\n    verbose::Bool=false,\n)::Nothing\n\nCreate relationship records between a registered GenomicBreeding dataset and a database entity table.\n\nThe function populates a junction table linking a registered Genomes or Phenomes dataset to records stored in another database table, such as entries, traits, sites, treatments, or other supported entities. The dataset type is inferred from the relationship table name and the corresponding JLD2 file is loaded to extract relationship values.\n\nValues extracted from the dataset are optionally transformed using link_value_parser, matched against records in the corresponding database table, and inserted into the specified relationship table. Existing relationships are preserved through the use of ON CONFLICT DO NOTHING.\n\nFor Genomes datasets, relationship values are extracted directly from the property corresponding to the target table, i.e. \"entries\". For Phenomes  datasets, values are extracted from the traits field and may be parsed into  other entities using link_value_parser. This allows relationship tables  such as phenomes_sites and phenomes_treatments to be populated when trait  names follow a consistent naming convention containing embedded metadata  separated by delimiters (for example \"Trait|Treatment-Site-Year\"). In these  cases, link_value_parser can be used to extract the relevant component prior to database matching.\n\nAll insert operations are performed within a single transaction. If an error occurs during processing, the transaction is rolled back and the exception is re-raised.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ntable::String: Name of the relationship table to populate. Must correspond to a valid table matching the pattern genomes_* or phenomes_*.\nfname_jld2::String: Path to a registered JLD2 file containing a Genomes or Phenomes object.\nlink_value_parser::Function=x -> String(split(x, '|')[1]): Function used to transform extracted values before matching them against database records.\nverbose::Bool=false: If true, display progress information and summary statistics during processing.\n\nReturns\n\nNothing: Relationship records are inserted into the specified relationship table.\n\nThrows\n\nErrorException: If the database connection has been closed.\nErrorException: If table is not a valid relationship table.\nErrorException: If the supplied JLD2 file does not exist.\nErrorException: If the supplied file does not appear to contain the expected object type.\nErrorException: If the associated dataset has not been registered in the database.\nAny database exception raised during processing is rethrown after transaction rollback.\nAny exception raised while loading the JLD2 dataset.\n\nWarnings\n\nA warning is emitted when values extracted from the dataset cannot be found in the corresponding database table.\nUnmatched values are skipped and no relationship records are created for them.\nAn incorrect link_value_parser may result in otherwise valid relationships being omitted.\n\nNotes\n\nConnection validation is performed using check(conn).\nValid relationship tables are discovered automatically from existing database tables whose names match genomes_* or phenomes_*.\nThe dataset type is inferred from the relationship table prefix:\ngenomes_* → Genomes\nphenomes_* → Phenomes\nFile validation is performed using check(type; fname=fname_jld2).\nThe dataset must already be registered in the corresponding table (genomes or phenomes).\nDataset records are identified using the stored absolute file path.\nFor Genomes, relationship values are extracted directly from the target property.\nFor Phenomes, relationship values are extracted from the traits field and transformed using link_value_parser.\nThis design allows a single Phenomes dataset to populate multiple relationship tables (e.g. phenomes_traits, phenomes_sites, phenomes_treatments) when trait names encode multiple pieces of metadata in a consistent format.\nDuplicate values are removed prior to processing.\nRelationship records are inserted using ON CONFLICT DO NOTHING.\nAll database modifications occur within a transaction using BEGIN, COMMIT, and ROLLBACK.\nWhen verbose=true, progress information and summary statistics describing inserted, skipped, and unmatched records are displayed.\nThis function provides a generic mechanism for populating relationship tables involving Genomes and Phenomes datasets.\n\nExamples\n\njulia> fname_reference_genome = string(\"simulated_reference_genome-\", Dates.now(),\".fa\");\n\njulia> fname_genomes_jld2 = string(\"simulated_genotype_jld2-\", Dates.now(),\".jld2\");\n\njulia> fname_phenomes_jld2 = string(\"simulated_phenotype_jld2-\", Dates.now(),\".jld2\");\n\njulia> conn = dbconnect();\n\njulia> n = execute(conn, \"SELECT * FROM genomes_entries\") |> DataFrame |> nrow;\n\njulia> simulate_genomes(n=n+1, fname_reference_genome=fname_reference_genome, fname_genomes_jld2=fname_genomes_jld2) |> simulate_trials |> x -> simulate_phenomes(x, fname_phenomes_jld2=fname_phenomes_jld2);\n\njulia> upload_trial_data!(conn, fname=\"simulated_trials.tsv\", species=\"Acacia neglecta\", experiment=\"some-exp\", treatment=\"some_trt\", entry_type=\"family\", population_type=\"population\", relationship_type=\"member_of\");\n\njulia> upload_reference_genome!(conn, fname=abspath(fname_reference_genome), name=fname_reference_genome, notes=\"simulated\");\n\njulia> upload_genomes!(conn, fname=abspath(fname_genomes_jld2), name=fname_genomes_jld2, notes=\"simulated\", fname_reference_genome=abspath(fname_reference_genome));\n\njulia> upload_phenomes!(conn, fname=abspath(fname_phenomes_jld2), name=fname_phenomes_jld2, notes=\"simulated\");\n\njulia> n_before = execute(conn, \"SELECT * FROM genomes_entries\") |> DataFrame |> nrow;\n\njulia> define_relationships!(conn, table=\"genomes_entries\", fname_jld2=abspath(fname_genomes_jld2));\n\njulia> n_after = execute(conn, \"SELECT * FROM genomes_entries\") |> DataFrame |> nrow;\n\njulia> n_before < n_after\ntrue\n\njulia> n_before = execute(conn, \"SELECT * FROM phenomes_traits\") |> DataFrame |> nrow;\n\njulia> define_relationships!(conn, table=\"phenomes_traits\", fname_jld2=abspath(fname_phenomes_jld2));\n\njulia> n_after = execute(conn, \"SELECT * FROM phenomes_traits\") |> DataFrame |> nrow;\n\njulia> n_before < n_after\ntrue\n\njulia> n_before = execute(conn, \"SELECT * FROM phenomes_sites\") |> DataFrame |> nrow;\n\njulia> link_value_parser = x -> String(split(split(x, '|')[2], \"-\")[end-1]);\n\njulia> define_relationships!(conn, table=\"phenomes_sites\", fname_jld2=abspath(fname_phenomes_jld2), link_value_parser=link_value_parser);\n\njulia> n_after = execute(conn, \"SELECT * FROM phenomes_sites\") |> DataFrame |> nrow;\n\njulia> n_before < n_after\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.define_relationships!"},{"category":"method","location":"#GenomicBreedingDB.delete_names!-Tuple{LibPQ.Connection}","page":"Home","text":"delete_names!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    table::String,\n    df_col::String,\n    verbose::Bool=false,\n)::Nothing\n\nDelete records from a database table by matching names contained in a DataFrame column.\n\nThe function extracts unique names from the specified DataFrame column and removes matching records from the target database table. Deletion criteria are constructed using Filter objects and translated into parameterised SQL statements via concat_filters, ensuring consistent filtering behaviour across database operations.\n\nAll delete operations are performed within a single transaction. If an error occurs during processing, the transaction is rolled back and the original exception is rethrown.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing names to be removed.\ntable::String: Name of the target database table.\ndf_col::String: Name of the DataFrame column containing names to delete.\nverbose::Bool=false: If true, display progress information and a summary of deleted records.\n\nReturns\n\nNothing: Matching records are removed from the database.\n\nThrows\n\nErrorException: If df_col does not exist in the DataFrame.\nAny database exception raised during deletion is rethrown after the transaction is rolled back.\n\nNotes\n\nNames are converted to strings, sorted, and deduplicated before processing.\nDelete statements are generated using Filter and concat_filters.\nSQL parameters are supplied separately from the query text to support safe, parameterised execution.\nDelete operations are wrapped in a transaction using BEGIN, COMMIT, and ROLLBACK.\nProgress reporting is available when verbose=true.\nThe function attempts a delete operation for each supplied name regardless of whether a matching record exists.\nThe function permanently removes matching records from the specified table.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> df = simulate_genomes() |> simulate_trials |> tabularise;\n\njulia> df.entries = string.(\"test_delete_names-\", Dates.time() |> x -> replace(string(x), \".\" => \"_\"), \"-\", df.entries);\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"entries\");\n\njulia> df_before = execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> delete_names!(conn, df=df, table=\"entries\", df_col=\"entries\");\n\njulia> df_after = execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> nrow(df_before) > nrow(df_after)\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.delete_names!"},{"category":"method","location":"#GenomicBreedingDB.extract_all_tables-Tuple{LibPQ.Connection}","page":"Home","text":"extract_all_tables(\n    conn::LibPQ.Connection,\n)::DataFrame\n\nList all user-defined database tables together with their estimated row counts.\n\nThe function validates that the supplied database connection is open and then queries PostgreSQL system statistics to retrieve the names of all user tables and their corresponding estimated number of live rows. Results are returned as a sorted DataFrame.\n\nThis function is useful for inspecting the current database contents, verifying that expected tables exist, and obtaining a quick overview of table sizes without executing potentially expensive row-count queries.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\n\nReturns\n\nDataFrame: Table containing the fields table_name and estimated_row_count.\n\nThrows\n\nErrorException: If the database connection has been closed.\nAny database exception raised while querying PostgreSQL system statistics.\n\nNotes\n\nConnection validation is performed using check(conn).\nTable information is obtained from PostgreSQL's pg_stat_user_tables system view.\nRow counts are estimates based on database statistics and may not exactly match the result of COUNT(*).\nOnly user-defined tables are included in the output.\nResults are sorted prior to being returned.\nThe function performs a read-only query and does not modify the database.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> extract_all_tables(conn) |> nrow > 0\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.extract_all_tables"},{"category":"method","location":"#GenomicBreedingDB.extract_environment_variables-Tuple{DataFrames.DataFrame}","page":"Home","text":"extract_environment_variables(\n    df::DataFrame;\n    verbose::Bool=false,\n)::Vector{String}\n\nIdentify environmental variable columns in a DataFrame and return their names.\n\nThe function determines candidate environmental variables by excluding known identifier and metadata columns. Candidate names are validated to ensure they do not contain illegal strings and are then filtered to retain only columns containing at least one valid numeric value.\n\nColumns containing only missing, NaN, or infinite values are excluded from the result. If no valid environmental variables remain after filtering, an error is raised describing the rejected candidates.\n\nArguments\n\ndf::DataFrame: Environmental data containing identifier columns and one or more environmental variables.\nverbose::Bool=false: If true, print the detected environmental variable names to standard output.\n\nReturns\n\nVector{String}: Names of columns identified as valid environmental variables.\n\nThrows\n\nErrorException: If candidate variable names contain illegal strings.\nErrorException: If no valid numeric environmental variables are found.\n\nNotes\n\nKnown identifier and metadata columns such as measurements, sites, replications, blocks, rows, cols, experiments, treatments, dates, and layouts are automatically excluded.\nEnvironmental variables must contain at least one non-missing, finite numeric value to be considered valid.\nColumns consisting entirely of missing, NaN, or infinite values are excluded.\nReturned names preserve the order in which columns appear in the input DataFrame.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials |> simulate_environments;\n\njulia> df = load_environments_df(\"simulated_environments.tsv\");\n\njulia> sort(extract_environment_variables(df)) == sort(names(df)[3:end])\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.extract_environment_variables"},{"category":"method","location":"#GenomicBreedingDB.extract_ids-Tuple{LibPQ.Connection}","page":"Home","text":"extract_ids(\n    conn::LibPQ.Connection;\n    names::Vector{String},\n    table::String,\n    is_like::Bool=false,\n)::DataFrame\n\nRetrieve identifier and name pairs from a database table for a collection of names.\n\nWhen is_like is false, names are matched exactly using a SQL ANY condition. When is_like is true, each provided name is matched using a case-insensitive ILIKE search, allowing partial matches. Matching records are returned as a DataFrame containing id and name columns.\n\nThe function validates that the specified table exists and contains a name column before executing any queries. Missing identifiers are removed from the result and all returned values are converted to String.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nnames::Vector{String}: Names to search for in the specified table.\ntable::String: Name of the table containing id and name fields.\nis_like::Bool=false: If true, perform case-insensitive partial matching using ILIKE; otherwise perform exact matching.\n\nReturns\n\nDataFrame: Table containing matching id and name pairs.\n\nNotes\n\nThe specified table must contain both id and name columns.\nPartial matching is performed individually for each element in names.\nUnderscore characters in search strings are escaped when using ILIKE to prevent unintended wildcard matching.\nRows with missing identifiers are removed from the returned result.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> df.entries = string.(\"test_extract_ids_1_784863659192497e9-\", df.entries);\n\njulia> conn = dbconnect();\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"entries\")\n\njulia> df_entries = extract_ids(conn, names=sort(unique(df.entries)), table=\"entries\");\n\njulia> sort(df_entries.name) == sort(unique(df.entries))\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.extract_ids"},{"category":"method","location":"#GenomicBreedingDB.extract_names-Tuple{LibPQ.Connection}","page":"Home","text":"extract_names(\n    conn::LibPQ.Connection;\n    ids::Vector{String},\n    table::String,\n)::DataFrame\n\nRetrieve name values associated with a collection of identifiers from a database table.\n\nThe function queries the specified table for records whose identifiers match the provided ids and returns the corresponding id and name pairs as a DataFrame. The table and required name column are validated before the query is executed.\n\nRows with missing identifiers are removed from the result, and both id and name columns are converted to String values before being returned.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nids::Vector{String}: Identifiers to look up in the specified table.\ntable::String: Name of the table containing id and name fields.\n\nReturns\n\nDataFrame: Table containing matching id and name pairs.\n\nNotes\n\nThe specified table must exist and contain a name column.\nIdentifier matching is performed using a parameterised SQL ANY condition.\nRows with missing identifiers are excluded from the returned result.\nReturned id and name values are converted to String for consistency.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> df.entries = string.(\"test_extract_names_1_784863659201014e9-\", df.entries);\n\njulia> conn = dbconnect();\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"entries\")\n\njulia> df_entries_0 = extract_ids(conn, names=sort(unique(df.entries)), table=\"entries\");\n\njulia> df_entries_1 = extract_names(conn, ids=df_entries_0.id, table=\"entries\");\n\njulia> close(conn);\n\njulia> df_entries_0 == df_entries_1\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.extract_names"},{"category":"method","location":"#GenomicBreedingDB.extract_table_contents-Tuple{LibPQ.Connection, String}","page":"Home","text":"extract_table_contents(\n    conn::LibPQ.Connection,\n    table::String,\n)::DataFrame\n\nExtract all records from a database table and return them as a DataFrame.\n\nThe function validates that the supplied database connection is open and confirms that the specified table exists before executing a SELECT * query. All rows and columns from the table are retrieved and returned without modification.\n\nThis function provides a convenient way to inspect, export, or explore the contents of a database table in tabular form.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ntable::String: Name of the table to extract.\n\nReturns\n\nDataFrame: Complete contents of the specified database table.\n\nThrows\n\nErrorException: If the database connection has been closed.\nErrorException: If the specified table does not exist.\nAny database exception raised whilst executing the query.\n\nNotes\n\nConnection validation is performed using check(conn).\nTable validation is performed using check(conn, table).\nThe query retrieves all rows and all columns using SELECT *.\nNo filtering, sorting, or column selection is applied.\nThe function performs a read-only operation and does not modify the database.\nLarge tables may require substantial memory to load into a DataFrame.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> extract_table_contents(conn, \"entries\") |> nrow > 0\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.extract_table_contents"},{"category":"method","location":"#GenomicBreedingDB.extract_traits-Tuple{DataFrames.DataFrame}","page":"Home","text":"extract_traits(\n    df::DataFrame;\n    verbose::Bool=false,\n)::Vector{String}\n\nIdentify trait columns in a trial DataFrame and return their names.\n\nThe function determines candidate trait columns by excluding known metadata fields and standard trial attributes. Candidate names are validated to ensure they do not contain illegal strings and are then filtered to retain only columns containing at least one valid numeric value.\n\nColumns named id or ending with _id are excluded from consideration. Columns containing only missing, NaN, or infinite values are also removed. If no valid trait columns remain after filtering, an error is raised describing the rejected candidates.\n\nArguments\n\ndf::DataFrame: Trial data containing metadata columns and one or more trait columns.\nverbose::Bool=false: If true, print the detected trait names to standard output.\n\nReturns\n\nVector{String}: Names of columns identified as valid trait variables.\n\nThrows\n\nErrorException: If candidate trait names contain illegal strings.\nErrorException: If no valid numeric trait columns are found.\n\nNotes\n\nStandard trial-related columns defined by Trials are automatically excluded.\nAdditional metadata columns such as dates, species, experiments, treatments, and related descriptors are excluded from trait detection.\nColumns named id or matching the pattern *_id are ignored.\nA trait column must contain at least one non-missing, finite numeric value to be considered valid.\nReturned trait names are sorted according to the order they appear in the input DataFrame.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> traits = extract_traits(df);\n\njulia> traits == filter(x -> !isnothing(match(Regex(\"^trait_\"), x)), names(df))\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.extract_traits"},{"category":"method","location":"#GenomicBreedingDB.insert_entry_relationships!-Tuple{LibPQ.Connection}","page":"Home","text":"insert_entry_relationships!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    verbose::Bool=false,\n)::Nothing\n\nInsert relationships between entries and populations into the entry_relationships database table.\n\nThe function extracts unique combinations of entries, populations, and relationship types from the supplied DataFrame and inserts them into the database. Entry and population names are first resolved to their corresponding identifiers in the entries table before relationship records are created.\n\nAll insert operations are performed within a single transaction. Existing relationships are ignored using an ON CONFLICT DO NOTHING clause, allowing the function to be safely re-run without creating duplicate records.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing relationship information.\nverbose::Bool=false: If true, display progress information and summary messages during insertion.\n\nReturns\n\nNothing: Relationship records are inserted into the database.\n\nThrows\n\nErrorException: If the entry_relationships table does not exist.\nErrorException: If any required columns are missing from the DataFrame.\nErrorException: If an invalid relationship type is encountered.\nAny database exception raised during insertion is rethrown after the transaction is rolled back.\n\nNotes\n\nThe input DataFrame must contain the columns entries, populations, and relationship_types.\nSupported relationship types are: member_of, clone_of, parent_is, maternal_parent_is, and paternal_parent_is.\nEntry and population names are resolved to identifiers using lookups against the entries table.\nInsert operations are wrapped in a transaction using BEGIN, COMMIT, and ROLLBACK.\nExisting records are preserved through the use of ON CONFLICT (child_id, parent_id, rel_type) DO NOTHING.\nProgress reporting is available when verbose=true.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> df.entries = string.(\"test_entry_rels-\", Dates.time() |> x -> replace(string(x), \".\" => \"_\"), \"-\", df.entries);\n\njulia> df.populations = string.(\"test_entry_rels-\", Dates.time() |> x -> replace(string(x), \".\" => \"_\"), \"-\", df.populations);\n\njulia> df[!, \"relationship_types\"] .= \"member_of\";\n\njulia> conn = dbconnect();\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"entries\");\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"populations\");\n\njulia> df_before = LibPQ.execute(conn, \"SELECT * FROM entry_relationships\") |> DataFrame;\n\njulia> insert_entry_relationships!(conn, df=df);\n\njulia> df_after = LibPQ.execute(conn, \"SELECT * FROM entry_relationships\") |> DataFrame;\n\njulia> close(conn);\n\njulia> nrow(df_before) < nrow(df_after)\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.insert_entry_relationships!"},{"category":"method","location":"#GenomicBreedingDB.insert_environment_data!-Tuple{LibPQ.Connection}","page":"Home","text":"insert_environment_data!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    environment_variables::Vector{String},\n    verbose::Bool=false,\n)::Nothing\n\nInsert environmental observations from a DataFrame into the environment_data database table. This function is called by upload_environment_data!(...) which  handles the input file loading and DataFrame preparation.\n\nThe function resolves identifiers for experiments, sites, treatments, layouts, measurements, and environmental variables before importing environmental observations into the database. For each row in the input DataFrame and each specified environmental variable, a record is created linking the relevant experimental factors to the observed value.\n\nAll insert operations are performed within a single transaction. Existing records are ignored using an ON CONFLICT DO NOTHING clause, allowing repeated imports without creating duplicate observations.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing environmental observations and associated metadata.\nenvironment_variables::Vector{String}: Names of environmental variable columns to import.\nverbose::Bool=false: If true, display progress information during import.\n\nReturns\n\nNothing: Environmental records are inserted into the database.\n\nThrows\n\nErrorException: If the environment_data table does not exist.\nErrorException: If any required reference table has not been initialised.\nAny database exception raised during insertion is rethrown after the transaction is rolled back.\n\nNotes\n\nThe following reference tables must be populated before import: experiments, sites, treatments, layouts, measurements, and environment_variables.\nIdentifier values are resolved using extract_ids prior to data insertion.\nMissing environmental values are stored as NaN.\nOne database record is generated for each combination of DataFrame row and environmental variable.\nInsert operations are wrapped in a transaction using BEGIN, COMMIT, and ROLLBACK.\nExisting records are preserved through the use of a composite ON CONFLICT DO NOTHING constraint.\nProgress reporting is available when verbose=true.\nThe function assumes all environmental variable names have already been registered in the environment_variables table.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials |> simulate_environments;\n\njulia> df = load_environments_df(\"simulated_environments.tsv\");\n\njulia> conn = dbconnect();\n\njulia> add_col!(df, col = \"replications\", value = \"1\"); add_col!(df, col = \"blocks\", value = \"1\"); add_col!(df, col = \"rows\", value = \"1\"); add_col!(df, col = \"cols\", value = \"1\"); add_col!(df, col = \"experiments\", value = \"exp-42\"); add_col!(df, col = \"treatments\", value = \"trt-42\");\n\njulia> add_measurement_dates!(df);\n\njulia> insert_layouts!(conn, df = df, is_trial = false); insert_names!(conn, df = df, table = \"experiments\", df_col = \"experiments\"); insert_names!(conn, df = df, table = \"treatments\", df_col = \"treatments\"); insert_names!(conn, df = df, table = \"sites\", df_col = \"sites\"); insert_names!(conn, df = df, table = \"measurements\", df_col = \"measurements\");\n\njulia> environment_variables = extract_environment_variables(df);\n\njulia> insert_names!(conn, df = DataFrame(environment_variables = environment_variables), table = \"environment_variables\", df_col = \"environment_variables\");\n\njulia> insert_environment_data!(conn, df=df, environment_variables=environment_variables);\n\njulia> execute(conn, \"SELECT id,value FROM environment_data\") |> DataFrame |> nrow > 0\ntrue\n\njulia> close(conn);\n\n\n\n\n\n\n","title":"GenomicBreedingDB.insert_environment_data!"},{"category":"method","location":"#GenomicBreedingDB.insert_layouts!-Tuple{LibPQ.Connection}","page":"Home","text":"insert_layouts!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    is_trial::Bool=true,\n    verbose::Bool=false,\n)::Nothing\n\nInsert unique layout definitions from a DataFrame into the layouts database table.\n\nThe function parses layout information from the input DataFrame, extracts unique layout combinations, and inserts them into the layouts table. Each layout is represented by a composite name of the form \"<replication>-<block>-<row>-<col>\" together with its individual component values.\n\nAll insert operations are performed within a single database transaction. Existing layouts are ignored using an ON CONFLICT DO NOTHING clause, ensuring that repeated imports do not generate duplicate records.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing layout information.\nis_trial::Bool=true: If true, validate and parse the DataFrame as trial data before extracting layouts.\nverbose::Bool=false: If true, display progress information and summary messages during insertion.\n\nReturns\n\nNothing: Layout information is inserted into the database and the input DataFrame may be modified in place during parsing.\n\nThrows\n\nErrorException: If the layouts table does not exist.\nErrorException: If layout information cannot be parsed from the DataFrame.\nAny database exception raised during insertion is rethrown after the transaction is rolled back.\n\nNotes\n\nLayout identifiers are generated and standardised using parse_layouts!.\nEach unique layout is inserted only once.\nInsert operations are wrapped in a transaction using BEGIN, COMMIT, and ROLLBACK.\nExisting records are preserved through the use of ON CONFLICT (name) DO NOTHING.\nProgress reporting is available when verbose=true.\nThe function performs database writes but does not return the inserted records.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> conn = dbconnect();\n\njulia> insert_layouts!(conn, df=df);\n\njulia> n = execute(conn, \"SELECT * FROM layouts\") |> DataFrame |> nrow;\n\njulia> m = string.(df.replications, \"-\", df.blocks, \"-\", df.rows, \"-\", df.cols) |> unique |> length;\n\njulia> n >= m\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.insert_layouts!"},{"category":"method","location":"#GenomicBreedingDB.insert_names!-Tuple{LibPQ.Connection}","page":"Home","text":"insert_names!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    table::String,\n    df_col::String,\n    verbose::Bool=false,\n)::Nothing\n\nInsert unique names from a DataFrame column into a database table.\n\nThe function extracts unique values from the specified DataFrame column and inserts them into the name field of the target database table. Existing names are ignored using an ON CONFLICT DO NOTHING clause, allowing the operation to be safely repeated without creating duplicate records.\n\nAll inserts are performed within a database transaction. If an error occurs during insertion, the transaction is rolled back and the original exception is rethrown.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing names to be inserted.\ntable::String: Name of the target database table.\ndf_col::String: Name of the DataFrame column containing the names to insert.\nverbose::Bool=false: If true, display progress information and a summary of inserted records.\n\nReturns\n\nNothing: Names are inserted into the database and the input DataFrame is not modified.\n\nThrows\n\nErrorException: If the specified table does not exist.\nErrorException: If the table does not contain a name column.\nErrorException: If df_col is not present in the DataFrame.\nErrorException: If one or more values in df_col contain illegal strings.\nAny database exception raised during insertion is rethrown after the transaction is rolled back.\n\nNotes\n\nThe target table must contain a unique constraint on the name column for conflict handling to work correctly.\nValues are converted to strings, sorted, and deduplicated before insertion.\nInserts are wrapped in a single transaction using BEGIN, COMMIT, and ROLLBACK.\nExisting records are preserved through the use of ON CONFLICT (name) DO NOTHING.\nProgress reporting is available when verbose=true.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> df.entries = string.(\"test_insert_names-\", Dates.time() |> x -> replace(string(x), \".\" => \"_\"), \"-\", df.entries);\n\njulia> conn = dbconnect();\n\njulia> df_before = LibPQ.execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"entries\");\n\njulia> df_after = LibPQ.execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> close(conn);\n\njulia> nrow(df_before) < nrow(df_after)\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.insert_names!"},{"category":"method","location":"#GenomicBreedingDB.insert_phenotype_data!-Tuple{LibPQ.Connection}","page":"Home","text":"insert_phenotype_data!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    traits::Vector{String},\n    verbose::Bool=false,\n)::Nothing\n\nInsert phenotype observations from a DataFrame into the phenotype_data database table. This function is called by upload_trial_data!(...) which handles the input file loading and DataFrame preparation.\n\nThe function resolves identifiers for entries, experiments, sites, treatments, layouts, measurements, and traits before importing phenotype values into the database. For each row in the input DataFrame and each specified trait, a phenotype record is created linking the relevant experimental factors to the observed trait value.\n\nAll insert operations are performed within a single transaction. Existing phenotype records are ignored using an ON CONFLICT DO NOTHING clause, allowing repeated imports without creating duplicate observations.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing phenotype observations and associated metadata.\ntraits::Vector{String}: Names of trait columns to import as phenotype data.\nverbose::Bool=false: If true, display progress information during import.\n\nReturns\n\nNothing: Phenotype records are inserted into the database.\n\nThrows\n\nErrorException: If the phenotype_data table does not exist.\nErrorException: If any required reference table has not been initialised.\nAny database exception raised during insertion is rethrown after the transaction is rolled back.\n\nNotes\n\nThe following reference tables must be populated before import: entries, experiments, sites, treatments, layouts, measurements, and traits.\nIdentifier values are resolved using extract_ids prior to data insertion.\nMissing phenotype values are stored as NaN.\nOne database record is generated for each combination of DataFrame row and trait.\nInsert operations are wrapped in a transaction using BEGIN, COMMIT, and ROLLBACK.\nExisting records are preserved through the use of a composite ON CONFLICT DO NOTHING constraint.\nProgress reporting is available when verbose=true.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> conn = dbconnect();\n\njulia> df.entries = string.(\"test_phenodat-\", Dates.time() |> x -> replace(string(x), \".\" => \"_\"), \"-\", df.entries);\n\njulia> [df[!, name] .= string(\"test_insert_phenodat_\", name) for name in [\"experiments\", \"treatments\"]];\n\njulia> parse_layouts!(df); insert_layouts!(conn, df=df);\n\njulia> [insert_names!(conn, df=df, table=name, df_col=name) for name in [\"entries\", \"experiments\", \"sites\", \"treatments\", \"measurements\"]];\n\njulia> traits = extract_traits(df); insert_names!(conn, df=DataFrame(traits=traits), table=\"traits\", df_col=\"traits\");\n\njulia> insert_phenotype_data!(conn, df=df, traits=[\"trait_1\"]);\n\njulia> df_exported = execute(conn, \"SELECT * FROM phenotype_data\") |> DataFrame;\n\njulia> ids = Dict(); [ids[x] = Dict(\"df\" => extract_ids(conn, names=unique(df[!, x]), table=x), \"name\" => x == \"entries\" ? \"entry\" : join(collect(x)[1:(end-1)]) ) for x in [x == \"entrys\" ? \"entries\" : x for x in replace.(names(df_exported)[2:(end-4)], \"_id\" => \"s\")]];\n\njulia> [v[\"df\"] = rename(v[\"df\"], [\"id\" => string(v[\"name\"], \"_id\"), \"name\" => v[\"name\"]]) for (k, v) in ids];\n\njulia> for (k, v) in ids; df_exported = innerjoin(df_exported, v[\"df\"], on=string(v[\"name\"], \"_id\")); end\n\njulia> ids_exported = [\"experiment\", \"site\", \"treatment\", \"layout\", \"measurement\", \"entry\", \"value\"]; select!(df_exported, ids_exported);\n\njulia> ids = [\"experiments\", \"sites\", \"treatments\", \"layouts\", \"measurements\", \"entries\", \"trait_1\"]; select!(df, ids);\n\njulia> df.trait_1[ismissing.(df.trait_1)] .= Inf;\n\njulia> df_exported.value[isnan.(df_exported.value)] .= Inf;\n\njulia> sort!(df_exported, ids_exported);\n\njulia> sort!(df, ids);\n\njulia> Matrix(df_exported) == Matrix(df)\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.insert_phenotype_data!"},{"category":"method","location":"#GenomicBreedingDB.load_environments_df-Tuple{Any}","page":"Home","text":"load_environments_df(\n    fname;\n    missing_strings::Vector{String}=[\n        \"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", \"\"\n    ],\n)::DataFrame\n\nLoad environmental data from a delimited file and return it as a DataFrame.\n\nThe function verifies that the specified file exists before reading it using CSV.read. Values matching any of the supplied missing-value strings are converted to missing during import.\n\nArguments\n\nfname: Path to the environmental data file.\nmissing_strings::Vector{String}=[\"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", \"\"]: Strings that should be interpreted as missing values when reading the file.\n\nReturns\n\nDataFrame: Environmental data loaded from the specified file.\n\nThrows\n\nErrorException: If the specified file does not exist.\n\nNotes\n\nData are imported using CSV.read.\nThe default missing value markers are \"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", and \"\".\nAny value matching an entry in missing_strings is converted to missing in the resulting DataFrame.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials |> simulate_environments;\n\njulia> df = load_environments_df(\"simulated_environments.tsv\");\n\njulia> prod(size(df)) > 0\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.load_environments_df"},{"category":"method","location":"#GenomicBreedingDB.load_trial_df-Tuple{String}","page":"Home","text":"load_trial_df(\n    fname::String;\n    missing_strings::Vector{String}=String[],\n)::DataFrame\n\nLoad trial data from a delimited file and return it as a DataFrame.\n\nThe function first verifies that the specified file exists. If no custom missing value strings are provided, it attempts to parse the file using GenomicBreedingIO.readdelimited and convert the result into a tabular representation. If this process fails, the file is read directly using CSV.read with a predefined set of missing value markers.\n\nWhen custom missing value strings are supplied, the file is read directly using CSV.read and the specified missing value definitions. If present, a column named #years is automatically renamed to years.\n\nArguments\n\nfname::String: Path to the trial data file.\nmissing_strings::Vector{String}=String[]: Strings that should be interpreted as missing values when reading the file.\n\nReturns\n\nDataFrame: Trial data loaded from the specified file.\n\nThrows\n\nErrorException: If the specified file does not exist.\n\nNotes\n\nWhen missing_strings is empty, the function preferentially uses GenomicBreedingIO.readdelimited followed by GenomicBreedingCore.tabularise.\nIf parsing with GenomicBreedingIO.readdelimited fails, the file is read using CSV.read as a fallback.\nThe default missing value markers are \"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", and \"\".\nA column named #years is renamed to years when present to improve compatibility with downstream processing.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> prod(size(df)) > 0\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.load_trial_df"},{"category":"method","location":"#GenomicBreedingDB.parse_layouts!-Tuple{DataFrames.DataFrame}","page":"Home","text":"parse_layouts!(\n    df::DataFrame;\n    is_trial::Bool=true,\n)::Nothing\n\nParse layout-related columns into integer values and generate a standardised layouts column.\n\nThe function converts the replications, blocks, rows, and cols columns to Int64 values when they are not already stored as integer vectors. Parsing is performed by extracting the trailing numeric component from strings containing underscore- or hyphen-delimited identifiers.\n\nAfter parsing, a composite layouts column is generated by concatenating the replication, block, row, and column values using the format \"<replication>-<block>-<row>-<col>\".\n\nArguments\n\ndf::DataFrame: DataFrame containing layout-related columns.\nis_trial::Bool=true: If true, validate the input using validate_trials before parsing.\n\nReturns\n\nNothing: The input DataFrame is modified in place.\n\nThrows\n\nErrorException: If any of the layout columns cannot be parsed into Int64 values.\n\nNotes\n\nThe columns replications, blocks, rows, and cols are expected to be present in the input DataFrame.\nColumns already stored as Vector{Int64} are left unchanged.\nString values are parsed by extracting the final token after splitting on _ and then -.\nA new or updated layouts column is created from the parsed integer values.\nThe function performs all modifications in place.\n\nExamples\n\njulia> df_ids = DataFrame(entries=repeat([\"\"],3), measurements=repeat([\"\"],3), populations=repeat([\"\"],3), seasons=repeat([\"\"],3), sites=repeat([\"\"],3), years=repeat([\"\"],3));\n\njulia> replications=[1, 2, 3]; blocks=[1, 1, 2]; rows=[1, 2, 3]; cols=[10, 11, 12];\n\njulia> df_expected = hcat(df_ids, DataFrame(replications=replications, blocks=blocks, rows=rows, cols=cols, layouts=string.(replications, \"-\", blocks, \"-\", rows, \"-\", cols)));\n\njulia> df_1 = copy(df_expected);\n\njulia> parse_layouts!(df_1)\n\njulia> df_1 == df_expected\ntrue\n\njulia> df_input = hcat(df_ids, DataFrame(replications=[\"rep_1\", \"rep_2\", \"3\"], blocks=[\"b_1\", \"1\", \"block-2\"], rows=[1, 2, 3], cols=[\"column-10\", \"COL-11\", \"column_12\"]));\n\njulia> df_2 = copy(df_input);\n\njulia> parse_layouts!(df_2)\n\njulia> df_2 == df_expected\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.parse_layouts!"},{"category":"method","location":"#GenomicBreedingDB.query_table-Tuple{LibPQ.Connection}","page":"Home","text":"query_table(\n    conn::LibPQ.Connection;\n    filters::Vector{Filter},\n    output_fields::Vector{String}=[\"*\"],\n    verbose::Bool=false,\n)::DataFrame\n\nQuery a database table using a collection of Filter objects and return the results as a DataFrame.\n\nThe target table is inferred from the first filter in filters. All supplied filters are validated and combined into a parameterised SQL WHERE clause. The resulting query is executed and returned as a DataFrame.\n\nColumns whose names end with _id are automatically resolved to their corresponding entity names by querying the associated lookup table. Identifier columns are replaced with the resolved names and renamed accordingly. For example, entry_id becomes entry.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfilters::Vector{Filter}: Collection of filters used to construct the WHERE clause.\noutput_fields::Vector{String}=[\"*\"]: Columns to include in the SELECT statement.\nverbose::Bool=false: If true, display progress information while resolving foreign-key fields.\n\nReturns\n\nDataFrame: Query results with foreign-key identifiers resolved to their corresponding names.\n\nNotes\n\nConnection validation is performed using check(conn).\nAll filters must reference the same table.\nSQL parameters are supplied separately from the query text to support safe, parameterised execution.\nForeign-key fields are identified using the _id suffix convention.\nAssociated lookup tables are inferred automatically from field names, for example site_id → sites.\nThe special case entry_id is resolved using the entries table.\nProgress reporting is available when verbose=true.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> table = \"phenotype_data\";\n\njulia> filters = Filter[];\n\njulia> push!(filters, Filter(conn, table=table, field=\"entry\", filter_like=\"1\"));\n\njulia> push!(filters, Filter(conn, table=table, field=\"site\", filter_in=[\"site_1\", \"site_2\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"value\", filter_between=(10, 20)));\n\njulia> df = query_table(conn, filters=filters);\n\njulia> prod(.!isnothing.(match.(Regex(\"1\"), df.entry))) == 1\ntrue\n\njulia> prod(.!isnothing.(match.(Regex(\"site_1|site_2\"), df.site))) == 1\ntrue\n\njulia> prod((df.value .>= 10) .&& (df.value .<= 20))\ntrue\n\njulia> table = \"environment_data\";\n\njulia> filters = Filter[];\n\njulia> push!(filters, Filter(conn, table=table, field=\"site\", filter_in=[\"site_1\", \"site_2\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"site\", filter_in=[\"site_1\", \"site_2\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"value\", filter_between=(10, 20)));\n\njulia> df = query_table(conn, filters=filters);\n\njulia> prod(.!isnothing.(match.(Regex(\"site_1|site_2\"), df.site))) == 1\ntrue\n\njulia> prod((df.value .>= 10) .&& (df.value .<= 20))\ntrue\n\njulia> df == query_table(conn, filters=unique(filters))\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.query_table"},{"category":"method","location":"#GenomicBreedingDB.simulate_environments-Tuple{Trials}","page":"Home","text":"simulate_environments(\n    trials::Trials;\n    fname_environments_tsv::String=\"simulated_environments.tsv\",\n    n_measurements_in_between_phenotypings::Int64=10,\n    sparsity::Float64=0.05,\n    overwrite::Bool=true,\n    verbose::Bool=false,\n)::DataFrame\n\nSimulate environmental measurements associated with a trial dataset and export the results to a tab-delimited file.\n\nThe function generates synthetic environmental observations for combinations of measurement dates and trial identifiers. Additional measurement dates are inserted between phenotyping events to create a more densely sampled environmental time series. Environmental variables such as rainfall, temperature, humidity, and solar radiation are then simulated and optionally populated with missing values according to the specified sparsity level.\n\nThe resulting environmental dataset is written to disk and returned as a DataFrame.\n\nArguments\n\ntrials::Trials: Trial dataset used to define the experimental structure and measurement schedule.\nfname_environments_tsv::String=\"simulated_environments.tsv\": Path to the output TSV file.\nn_measurements_in_between_phenotypings::Int64=10: Number of environmental measurement dates to generate between consecutive phenotyping dates.\nsparsity::Float64=0.05: Proportion of environmental observations to replace with missing.\noverwrite::Bool=true: If true, overwrite an existing output file.\nverbose::Bool=false: If true, display progress information during simulation.\n\nReturns\n\nDataFrame: Simulated environmental dataset.\n\nThrows\n\nErrorException: If the output file already exists and overwrite is false.\nErrorException: If the trial data do not contain a measurements column.\nErrorException: If trial measurement dates cannot be validated.\n\nNotes\n\nTrial data are first converted to a tabular representation using tabularise.\nEnvironmental records are generated for unique combinations of measurements, sites, experiments, and treatments when present.\nIntermediate measurement dates are interpolated between phenotyping events.\nSimulated environmental variables include rainfall, solar radiation, minimum temperature, maximum temperature, and humidity.\nMissing values are introduced randomly according to the specified sparsity level.\nResults are written as a tab-delimited file using CSV.write.\nExisting output files are removed before writing when overwrite=true.\n\nExamples\n\njulia> df_environments = simulate_genomes() |> simulate_trials |> simulate_environments;\n\njulia> prod(size(df_environments)) > 0\ntrue\n\njulia> isfile(\"simulated_environments.tsv\")\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.simulate_environments"},{"category":"method","location":"#GenomicBreedingDB.simulate_fit-Tuple{Genomes, Phenomes}","page":"Home","text":"simulate_fit(\n    genomes::Genomes,\n    phenomes::Phenomes;\n    fname_fit_jld2::String=\"simulated_fit.jld2\",\n    overwrite::Bool=true,\n)::Fit\n\nGenerate a simulated genomic prediction model from compatible genomic and phenomic datasets and export the result as a JLD2 file.\n\nThe function constructs a synthetic Fit object using genotype and phenotype information from the supplied Genomes and Phenomes objects. Marker effects are generated randomly, predictions are calculated from allele frequencies, and summary performance metrics are computed from the observed and predicted phenotypic values.\n\nThe resulting model is written to disk and returned for subsequent analysis or testing.\n\nArguments\n\ngenomes::Genomes: Genomic dataset containing marker information and allele frequencies.\nphenomes::Phenomes: Phenomic dataset containing trait measurements for the same set of entries.\nfname_fit_jld2::String=\"simulated_fit.jld2\": Path to the output JLD2 file.\noverwrite::Bool=true: If true, overwrite an existing output file.\n\nReturns\n\nFit: Simulated genomic prediction model.\n\nThrows\n\nErrorException: If the entries in Genomes and Phenomes do not align exactly.\n\nNotes\n\nGenomic and phenomic datasets must contain the same entries in the same order.\nMissing allele frequencies and phenotype values are replaced with 0.0 prior to model construction.\nThe simulated model is labelled as \"OLS\".\nMarker effects are generated randomly and are not estimated from the data.\nPredicted values are calculated as the matrix product of allele frequencies and simulated marker effects.\nModel performance metrics include correlation (cor), root mean squared error (rmse), and coefficient of determination (r2).\nThe resulting fit is exported using GenomicBreedingIO.writejld2.\n\nExamples\n\njulia> genomes = simulate_genomes();\n\njulia> phenomes = simulate_trials(genomes) |> simulate_phenomes;\n\njulia> fit = simulate_fit(genomes, phenomes);\n\njulia> checkdims(fit)\ntrue\n\njulia> isfile(\"simulated_fit.jld2\")\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.simulate_fit"},{"category":"method","location":"#GenomicBreedingDB.simulate_genomes-Tuple{}","page":"Home","text":"simulate_genomes(\n    ;\n    fname_reference_genome::String=\"simulated_reference_genome.fa\",\n    fname_genomes_vcf::String=\"simulated_genomes.vcf\",\n    fname_genomes_jld2::String=\"simulated_genomes.jld2\",\n    n::Int64=100,\n    l::Int64=10_000,\n    n_chroms::Int64=7,\n    n_alleles::Int64=2,\n    max_pos::Int64=135_000_000,\n    ld_corr_50perc_kb::Int64=0,\n    n_populations::Int64=3,\n    overwrite::Bool=true,\n    verbose::Bool=false,\n)::Genomes\n\nSimulate a population of genomes and export the results in multiple file formats.\n\nThe function generates a synthetic genomic dataset using GenomicBreedingCore.simulategenomes, creates a compatible simulated reference genome in FASTA format, and writes the simulated genomes to both VCF and JLD2 files. The generated Genomes object is returned for immediate use in downstream analyses.\n\nThe reference genome size is automatically determined from the maximum genomic position present in the simulated loci.\n\nArguments\n\nfname_reference_genome::String=\"simulated_reference_genome.fa\": Path to the output reference genome FASTA file.\nfname_genomes_vcf::String=\"simulated_genomes.vcf\": Path to the output VCF file.\nfname_genomes_jld2::String=\"simulated_genomes.jld2\": Path to the output JLD2 file.\nn::Int64=100: Number of individuals to simulate.\nl::Int64=10_000: Number of loci to simulate.\nn_chroms::Int64=7: Number of chromosomes.\nn_alleles::Int64=2: Number of alleles per locus.\nmax_pos::Int64=135_000_000: Maximum genomic position for simulated loci.\nld_corr_50perc_kb::Int64=0: Distance, in kilobases, at which linkage disequilibrium decays to approximately 50% correlation.\nn_populations::Int64=3: Number of populations to simulate.\noverwrite::Bool=true: If true, overwrite existing output files.\nverbose::Bool=false: If true, display progress and diagnostic messages during simulation.\n\nReturns\n\nGenomes: Simulated genomic dataset.\n\nNotes\n\nGenome simulation is performed using GenomicBreedingCore.simulategenomes.\nA reference genome is generated automatically using simulate_reference_genome.\nThe reference genome length is determined from the largest simulated locus position.\nSimulated genomes are exported to both VCF and JLD2 formats for compatibility with downstream workflows.\nExisting output files may be replaced when overwrite=true.\n\nExamples\n\njulia> genomes = simulate_genomes();\n\njulia> checkdims(genomes)\ntrue\n\njulia> isfile(\"simulated_reference_genome.fa\")\ntrue\n\njulia> isfile(\"simulated_genomes.vcf\")\ntrue\n\njulia> isfile(\"simulated_genomes.jld2\")\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.simulate_genomes"},{"category":"method","location":"#GenomicBreedingDB.simulate_phenomes-Tuple{Trials}","page":"Home","text":"simulate_phenomes(\n    trials::Trials;\n    fname_phenomes_jld2::String=\"simulated_phenomes.jld2\",\n    overwrite::Bool=true,\n)::Phenomes\n\nGenerate a phenomic dataset from a trial dataset and export the result as a JLD2 file.\n\nThe function extracts phenomic information from the supplied Trials object using extractphenomes, writes the resulting Phenomes object to disk, and returns it for immediate downstream use.\n\nExisting output files may be overwritten according to the value of overwrite.\n\nArguments\n\ntrials::Trials: Trial dataset from which phenomic data will be extracted.\nfname_phenomes_jld2::String=\"simulated_phenomes.jld2\": Path to the output JLD2 file.\noverwrite::Bool=true: If true, overwrite an existing output file.\n\nReturns\n\nPhenomes: Extracted phenomic dataset.\n\nThrows\n\nErrorException: If the output file already exists and overwrite is false.\n\nNotes\n\nPhenomic data are generated using extractphenomes.\nThe resulting dataset is exported using GenomicBreedingIO.writejld2.\nExisting output files are removed before writing when overwrite=true.\nThe returned Phenomes object is the same dataset written to disk.\n\nExamples\n\njulia> phenomes = simulate_genomes() |>  simulate_trials |> simulate_phenomes;\n\njulia> checkdims(phenomes)\ntrue\n\njulia> isfile(\"simulated_phenomes.jld2\")\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.simulate_phenomes"},{"category":"method","location":"#GenomicBreedingDB.simulate_reference_genome-Tuple{}","page":"Home","text":"simulate_reference_genome(\n    ;\n    fname_reference_genome::String=\"simulated_reference_genome.fa\",\n    n_bases::Int64=10_000,\n    n_chromosomes::Int64=7,\n    max_characters_per_line::Int64=80,\n    overwrite::Bool=true,\n)::Nothing\n\nGenerate a simulated reference genome in FASTA format.\n\nThe genome is composed of a specified number of chromosomes whose combined length equals n_bases. Chromosome sequences are generated by randomly sampling the DNA bases A, T, C, and G. Each chromosome is written as a separate FASTA record with a header of the form >chromN.\n\nSequence lines are wrapped according to max_characters_per_line to produce a standard FASTA-formatted output file.\n\nArguments\n\nfname_reference_genome::String=\"simulated_reference_genome.fa\": Path to the output FASTA file.\nn_bases::Int64=10_000: Total number of bases to generate across all chromosomes.\nn_chromosomes::Int64=7: Number of chromosomes to simulate.\nmax_characters_per_line::Int64=80: Maximum number of sequence characters per FASTA line.\noverwrite::Bool=true: If true, replace an existing output file.\n\nReturns\n\nNothing: The simulated genome is written to disk.\n\nThrows\n\nErrorException: If the output file already exists and overwrite is false.\n\nNotes\n\nChromosome lengths are generated by partitioning n_bases into n_chromosomes intervals.\nNucleotide bases are sampled uniformly from ['A', 'T', 'C', 'G'].\nExisting output files are removed before writing when overwrite=true.\nFASTA headers are written in the format >chrom1, >chrom2, ..., >chromN.\nThe function writes directly to disk and does not return the generated sequences.\n\nExamples\n\njulia> simulate_reference_genome(n_chromosomes=10);\n\njulia> io = open(\"simulated_reference_genome.fa\", \"r\");\n\njulia> [!isnothing(match(Regex(\"^>\"), line)) for line in eachline(io)] |> sum\n10\n\njulia> close(io);\n\n\n\n\n\n","title":"GenomicBreedingDB.simulate_reference_genome"},{"category":"method","location":"#GenomicBreedingDB.simulate_trials-Tuple{Genomes}","page":"Home","text":"simulate_trials(\n    genomes::Genomes;\n    fname_trials_tsv::String=\"simulated_trials.tsv\",\n    n_years::Int64=2,\n    n_seasons::Int64=4,\n    n_measurements::Int64=2,\n    n_sites::Int64=2,\n    n_replications::Int64=2,\n    sparsity::Float64=0.05,\n    overwrite::Bool=true,\n    verbose::Bool=false,\n)::Trials\n\nSimulate multi-environment trial data from a genomic dataset and export the results to a tab-delimited file.\n\nThe function generates synthetic trial data using a supplied Genomes object and a set of experimental design parameters. Simulated trials are written to disk as a TSV file and returned as a Trials object for immediate downstream use.\n\nPrior to simulation, the function verifies that the genomic data are valid and that existing output files may be overwritten according to the specified settings.\n\nArguments\n\ngenomes::Genomes: Genomic dataset used as the basis for trial simulation.\nfname_trials_tsv::String=\"simulated_trials.tsv\": Path to the output TSV file.\nn_years::Int64=2: Number of years to simulate.\nn_seasons::Int64=4: Number of seasons per year.\nn_measurements::Int64=2: Number of measurement events.\nn_sites::Int64=2: Number of trial sites.\nn_replications::Int64=2: Number of replications per trial.\nsparsity::Float64=0.05: Proportion of missing observations to introduce into the simulated dataset.\noverwrite::Bool=true: If true, overwrite existing output files.\nverbose::Bool=false: If true, display progress and diagnostic messages during simulation.\n\nReturns\n\nTrials: Simulated trial dataset.\n\nThrows\n\nErrorException: If the supplied Genomes object fails validation.\nErrorException: If the output file already exists and overwrite is false.\n\nNotes\n\nInput genomic data are validated using checkdims before simulation.\nTrial simulation is performed using GenomicBreedingCore.simulatetrials.\nSimulated trials are exported using GenomicBreedingIO.writedelimited.\nExisting output files are removed before writing when overwrite=true.\nThe returned Trials object is the same dataset written to disk.\n\nExamples\n\njulia> trials = simulate_genomes() |> simulate_trials;\n\njulia> checkdims(trials)\ntrue\n\njulia> isfile(\"simulated_trials.tsv\")\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.simulate_trials"},{"category":"method","location":"#GenomicBreedingDB.unstack_data_table-Tuple{DataFrames.DataFrame}","page":"Home","text":"unstack_data_table(df::DataFrame)::DataFrame\n\nConvert a validated long-format data table into a wide-format DataFrame.\n\nThe function identifies all columns except trait, environmental_variable, and value as row keys and uses either trait or environmental_variable as the column key. The selected key column is expanded into individual columns using unstack, with values sourced from the value column.\n\nThis transformation is useful for converting observational or measurement data from a normalised long format into an analysis-ready wide format where traits or environmental variables become separate columns.\n\nArguments\n\ndf::DataFrame: Long-format data table containing a value column and either a trait or environmental_variable column.\n\nReturns\n\nDataFrame: Wide-format table produced by unstacking the input data.\n\nNotes\n\nThe input is validated using validate_data_table before reshaping.\nExactly one of trait or environmental_variable is used as the column key.\nAll remaining columns, excluding the key and value columns, are used to define unique rows in the output.\nInternally, the transformation is performed using DataFrames.unstack.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> table = \"phenotype_data\";\n\njulia> filters = Filter[];\n\njulia> push!(filters, Filter(conn, table=table, field=\"entry\", filter_like=\"1\"));\n\njulia> push!(filters, Filter(conn, table=table, field=\"site\", filter_in=[\"site_1\", \"site_2\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"trait\", filter_in=[\"trait_2\", \"trait_3\"]));\n\njulia> push!(filters, Filter(conn, table=table, field=\"value\", filter_between=(10, 1_000)));\n\njulia> df = query_table(conn, filters=filters);\n\njulia> df_unstacked = unstack_data_table(df);\n\njulia> sum(.!isnothing.(match.(Regex(\"trait\"), names(df)))) < sum(.!isnothing.(match.(Regex(\"trait\"), names(df_unstacked))))\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.unstack_data_table"},{"category":"method","location":"#GenomicBreedingDB.update_table!-Tuple{LibPQ.Connection}","page":"Home","text":"update_table!(\n    conn::LibPQ.Connection;\n    filters::Vector{Filter},\n    destination_field::String,\n    value::Union{String,AbstractFloat,Int},\n    verbose::Bool=false,\n)::Nothing\n\nUpdate a single field in a database table for the record matching a set of filters.\n\nThe function constructs and executes a parameterised SQL UPDATE statement using the supplied filters to identify the target record. The specified destination_field is updated with the provided value.\n\nThe update is performed within a database transaction. To minimise the risk of unintended modifications, the function verifies that exactly one row was affected by the operation. If no rows or multiple rows are updated, the transaction is rolled back and an error is raised.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfilters::Vector{Filter}: Collection of filters used to identify the record to update.\ndestination_field::String: Name of the field to be updated.\nvalue::Union{String,AbstractFloat,Int}: New value to assign to the destination field.\nverbose::Bool=false: If true, display diagnostic information while constructing the query.\n\nReturns\n\nNothing: The database record is updated in place.\n\nThrows\n\nErrorException: If the supplied filters are invalid.\nErrorException: If the update affects zero rows.\nErrorException: If the update affects more than one row.\nAny database exception raised during query execution.\n\nNotes\n\nAll filters are validated using validate_filters before query execution.\nThe target table is inferred from the first filter in filters.\nSQL parameters are supplied separately from the query text to support safe, parameterised execution.\nDatabase modifications are performed within an explicit transaction using BEGIN, COMMIT, and ROLLBACK.\nThe function expects exactly one matching record and will fail if this condition is not satisfied.\nThis function performs a database modification and does not return the updated record.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> date_string = replace(string(time()), \".\" => \"_\");\n\njulia> df.species .= string(\"Homo sapiens-\", date_string);\n\njulia> conn = dbconnect();\n\njulia> insert_names!(conn, df=df, table=\"species\", df_col=\"species\");\n\njulia> df_before_1 = LibPQ.execute(conn, \"SELECT * FROM species WHERE name ILIKE '%Homo sapiens%'\") |> DataFrame;\n\njulia> df_before_2 = LibPQ.execute(conn, \"SELECT * FROM species WHERE name ILIKE '%Homo habilis%'\") |> DataFrame;\n\njulia> filters = [Filter(conn, table=\"species\", field=\"name\", filter_in=[string(\"Homo sapiens-\", date_string)])];\n\njulia> update_table!(conn, filters=filters, destination_field=\"name\", value=string(\"Homo habilis-\", date_string));\n\njulia> df_after_1 = LibPQ.execute(conn, \"SELECT * FROM species WHERE name ILIKE '%Homo sapiens%'\") |> DataFrame;\n\njulia> df_after_2 = LibPQ.execute(conn, \"SELECT * FROM species WHERE name ILIKE '%Homo habilis%'\") |> DataFrame;\n\njulia> (nrow(df_before_1) > nrow(df_after_1)) && (nrow(df_before_2) < nrow(df_after_2))\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.update_table!"},{"category":"method","location":"#GenomicBreedingDB.update_table_field_by_name!-Tuple{LibPQ.Connection}","page":"Home","text":"update_table_field_by_name!(\n    conn::LibPQ.Connection;\n    df::DataFrame,\n    table::String,\n    df_name_col::String,\n    df_source_col::String,\n    table_destination_field::String,\n    verbose::Bool=false,\n)::Nothing\n\nUpdate a field in a database table by matching records using their name values.\n\nThe function uses values from a DataFrame to update an existing database table. Rows are matched using the table's name field and values from df_name_col. The corresponding values from df_source_col are then written to table_destination_field.\n\nIf the destination field represents a foreign-key relationship, identified by a field name ending in _id, source values are automatically resolved to their corresponding identifiers in the related table before updates are applied.\n\nUpdates are performed individually using update_table!, which ensures that each update affects exactly one database record.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\ndf::DataFrame: DataFrame containing source values for the update.\ntable::String: Name of the table to update.\ndf_name_col::String: Column containing names used to match records in the database table.\ndf_source_col::String: Column containing values to write to the destination field.\ntable_destination_field::String: Name of the field to update in the target table.\nverbose::Bool=false: If true, display progress information and summary messages during the update process.\n\nReturns\n\nNothing: Records are updated directly in the database.\n\nThrows\n\nErrorException: If the target table does not exist.\nErrorException: If the table does not contain a name field.\nErrorException: If the destination field does not exist.\nErrorException: If either source column is missing from the DataFrame.\nErrorException: If the target table is empty.\nAny database exception raised during the update process.\n\nNotes\n\nThe target table must contain a name field used to identify records.\nDuplicate combinations of df_name_col and df_source_col are removed before processing.\nDestination fields ending in _id are interpreted as foreign keys and are resolved using extract_ids.\nRelated table names are inferred automatically from the destination field name.\nDate and DateTime values are converted to strings before being written to the database.\nUpdates are delegated to update_table!, which validates that exactly one record is modified for each operation.\nProgress reporting is available when verbose=true.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> df.entries = string.(\"test_update_table-\", time() |> x -> replace(string(x), \".\" => \"_\"), \"-\", df.entries);\n\njulia> df.species .= string.(\"test_update_table-\", time() |> x -> replace(string(x), \".\" => \"_\"), \"-Elysia chlorotica\");\n\njulia> conn = dbconnect();\n\njulia> insert_names!(conn, df=df, table=\"entries\", df_col=\"entries\");\n\njulia> insert_names!(conn, df=df, table=\"species\", df_col=\"species\");\n\njulia> df_before = LibPQ.execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> update_table_field_by_name!(conn, df=df, table=\"entries\", df_name_col=\"entries\", df_source_col=\"species\", table_destination_field=\"species_id\");\n\njulia> df_after = LibPQ.execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> sum(.!ismissing.(unique(df_before.species_id))) < sum(.!ismissing.(unique(df_after.species_id)))\ntrue\n\njulia> update_table_field_by_name!(conn, df=df, table=\"entries\", df_name_col=\"entries\", df_source_col=\"entries\", table_destination_field=\"name\");\n\njulia> df_before = deepcopy(df_after); df_after = LibPQ.execute(conn, \"SELECT * FROM entries\") |> DataFrame;\n\njulia> sum(.!ismissing.(unique(df_before.name))) == sum(.!ismissing.(unique(df_after.name)))\ntrue\n\njulia> idx_before = findall(df_before.name .== df.entries[1]); idx_after = findall(df_after.name .== df.entries[1]);\n\njulia> unique(df_before.updated_at[idx_before]) < unique(df_after.updated_at[idx_after]) # also shows that the update_at automatically updates\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.update_table_field_by_name!"},{"category":"method","location":"#GenomicBreedingDB.upload_environment_data!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_environment_data!(\n    conn::LibPQ.Connection;\n    fname::String,\n    missing_strings::Vector{String}=[\n        \"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", \"\"\n    ],\n    experiment::Union{Nothing,String}=nothing,\n    treatment::Union{Nothing,String}=nothing,\n    measurement_dates::Union{Nothing,Dict{String,String}}=nothing,\n    verbose::Bool=false,\n)::Nothing\n\nUpload environmental data from a file into the database and populate all associated reference and environmental data tables.\n\nThe function loads environmental data, validates the presence of required fields, adds missing metadata where necessary, identifies environmental variables, and imports all associated records into the database. This includes layouts, experiments, treatments, sites, measurements, environmental variables, and environmental observations.\n\nMissing spatial layout columns are automatically populated with default values when not present. Measurement dates may be supplied explicitly or inferred from measurement identifiers.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Path to the environmental data file.\nmissing_strings::Vector{String}=[\"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", \"\"]: Strings that should be interpreted as missing values when reading the file.\nexperiment::Union{Nothing,String}=nothing: Experiment name to assign when an experiments column is not present in the input data.\ntreatment::Union{Nothing,String}=nothing: Treatment name to assign when a treatments column is not present in the input data.\nmeasurement_dates::Union{Nothing,Dict{String,String}}=nothing: Optional mapping between measurement identifiers and measurement dates.\nverbose::Bool=false: If true, display progress information and diagnostic messages during processing.\n\nReturns\n\nNothing: Environmental data are inserted into the database.\n\nThrows\n\nErrorException: If the environmental data file cannot be loaded.\nErrorException: If either the measurements or sites column is missing from the input data.\nErrorException: If measurement dates are invalid or incomplete.\nErrorException: If environmental variables cannot be identified.\nAny database exception raised during the import process.\n\nNotes\n\nEnvironmental data are loaded using load_environments_df.\nThe columns measurements and sites are mandatory.\nMissing spatial columns (replications, blocks, rows, and cols) are automatically added with a default value of \"1\".\nMissing experiment and treatment metadata may be added using add_col!.\nMeasurement dates are validated or generated using add_measurement_dates!.\nEnvironmental variables are detected automatically using extract_environment_variables.\nLayout information is standardised and uploaded using insert_layouts!.\nReference tables are populated using insert_names!.\nEnvironmental observations are inserted using insert_environment_data!.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials |> simulate_environments;\n\njulia> conn = dbconnect();\n\njulia> upload_environment_data!(conn, fname=\"simulated_environments.tsv\", experiment=\"exp-1\", treatment=\"trt-42\");\n\njulia> execute(conn, \"SELECT id,value FROM environment_data\") |> DataFrame |> nrow > 0\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_environment_data!"},{"category":"method","location":"#GenomicBreedingDB.upload_fit!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_fit!(\n    conn::LibPQ.Connection;\n    fname::String,\n    name::String,\n    notes::String,\n)::Nothing\n\nUpload a fitted model dataset to the database and register its metadata.\n\nThe function validates that the supplied file exists and appears to be a JLD2 file containing a Fit object, verifies that the file path is absolute, and then registers the dataset in the fits table.\n\nFile validation is delegated to check(Fit; fname=...), which performs a lightweight inspection of the file to ensure it appears to contain a valid Fit object. Existing records are preserved using an ON CONFLICT DO NOTHING clause.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Absolute path to the JLD2 file containing a Fit object.\nname::String: Unique name used to identify the fitted model.\nnotes::String: Descriptive notes associated with the fitted model.\n\nReturns\n\nNothing: Metadata describing the fitted model are inserted into the database.\n\nThrows\n\nErrorException: If the supplied file does not exist.\nErrorException: If the file does not appear to contain a valid Fit object.\nErrorException: If the file path is not absolute.\nAny database exception raised during insertion.\n\nWarnings\n\nA warning is emitted when a matching fit record already exists and the insert operation is ignored.\n\nNotes\n\nFile validation is delegated to check(Fit; fname=...).\nOnly absolute file paths are accepted.\nRecords are inserted into the fits table using the supplied name, file path, and notes.\nExisting records are preserved through the use of ON CONFLICT DO NOTHING.\nThe JLD2 file itself is not stored in the database; only its metadata and file location are recorded.\nThis function is intended for registering previously generated fitted models for reproducibility, downstream prediction, and model evaluation workflows.\n\nExamples\n\njulia> fname_fit_jld2 = string(\"simulated_fit_jld2-\", Dates.now(),\".jld2\");\n\njulia> genomes = simulate_genomes(); phenomes = simulate_trials(genomes) |> simulate_phenomes;\n\njulia> simulate_fit(genomes, phenomes, fname_fit_jld2=fname_fit_jld2);\n\njulia> conn = dbconnect(); \n\njulia> upload_fit!(conn, fname=abspath(fname_fit_jld2), name=fname_fit_jld2, notes=\"simulated\");\n\njulia> query_table(conn, filters=[Filter(conn, table=\"fits\", field=\"name\", filter_in=[fname_fit_jld2])]) |> nrow == 1\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_fit!"},{"category":"method","location":"#GenomicBreedingDB.upload_genomes!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_genomes!(\n    conn::LibPQ.Connection;\n    fname::String,\n    name::String,\n    notes::String,\n    fname_reference_genome::String,\n)::Nothing\n\nUpload a Genomes dataset to the database and associate it with an existing reference genome.\n\nThe function validates that the supplied file exists and appears to be a JLD2 file containing a Genomes object, verifies that the file path is absolute, resolves the associated reference genome from the database, and then registers the genomic dataset in the genomes table.\n\nThe associated reference genome must already be registered in the database and is identified using its stored file path. Existing genome records are preserved using an ON CONFLICT DO NOTHING clause.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Absolute path to the JLD2 file containing a Genomes object.\nname::String: Unique name used to identify the genomic dataset.\nnotes::String: Descriptive notes associated with the genomic dataset.\nfname_reference_genome::String: Absolute path to a reference genome already registered in the reference_genomes table.\n\nReturns\n\nNothing: Metadata describing the genomic dataset are inserted into the database.\n\nThrows\n\nErrorException: If the supplied file does not exist.\nErrorException: If the file does not appear to contain a valid Genomes object.\nErrorException: If the file path is not absolute.\nErrorException: If the associated reference genome cannot be found in the database.\nAny database exception raised during insertion.\n\nWarnings\n\nA warning is emitted when a matching genome record already exists and the insert operation is ignored.\n\nNotes\n\nFile validation is delegated to check(Genomes; fname=...).\nOnly absolute file paths are accepted.\nThe associated reference genome must be registered beforehand using upload_reference_genome!.\nReference genome lookup is performed using the file_path field of the reference_genomes table.\nThe resolved reference genome identifier is stored in the genomes.reference_genome_id field.\nRecords are inserted into the genomes table using the supplied name, file path, reference genome identifier, and notes.\nExisting records are preserved through the use of ON CONFLICT DO NOTHING.\nThe JLD2 file itself is not stored in the database; only its metadata, file path, and reference genome association are recorded.\nThis function is intended for registering previously generated genomic datasets rather than creating new ones.\n\nExamples\n\njulia> fname_reference_genome = string(\"simulated_reference_genome-\", Dates.now(),\".fa\");\n\njulia> fname_genomes_jld2 = string(\"simulated_genotype_jld2-\", Dates.now(),\".jld2\");\n\njulia> simulate_genomes(fname_reference_genome=fname_reference_genome, fname_genomes_jld2=fname_genomes_jld2);\n\njulia> conn = dbconnect();\n\njulia> try isnothing(upload_genomes!(conn, fname=abspath(fname_genomes_jld2), name=fname_genomes_jld2, notes=\"simulated\", fname_reference_genome=abspath(fname_reference_genome))); catch; false; end\nfalse\n\njulia> upload_reference_genome!(conn, fname=abspath(fname_reference_genome), name=fname_reference_genome, notes=\"simulated\");\n\njulia> try isnothing(upload_genomes!(conn, fname=abspath(fname_genomes_jld2), name=fname_genomes_jld2, notes=\"simulated\", fname_reference_genome=abspath(fname_reference_genome))); catch; false; end\ntrue\n\njulia> query_table(conn, filters=[Filter(conn, table=\"genomes\", field=\"name\", filter_in=[fname_genomes_jld2])]) |> nrow == 1\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_genomes!"},{"category":"method","location":"#GenomicBreedingDB.upload_genotype_vcf!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_genotype_vcf!(\n    conn::LibPQ.Connection;\n    fname::String,\n    name::String,\n    notes::String,\n    fname_reference_genome::String,\n)::Nothing\n\nUpload a genotype VCF file to the database and associate it with an existing reference genome.\n\nThe function validates the supplied VCF file, verifies that its path is absolute, confirms that the associated reference genome has already been registered in the database, and then inserts a corresponding record into the genotype_vcfs table.\n\nValidation is performed by inspecting the VCF header and confirming the presence of the mandatory #CHROM line. Both uncompressed and gzip-compressed VCF files are supported.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Absolute path to the genotype VCF file.\nname::String: Unique name used to identify the genotype dataset.\nnotes::String: Descriptive notes associated with the genotype dataset.\nfname_reference_genome::String: Absolute path to a reference genome that has already been registered in the reference_genomes table.\n\nReturns\n\nNothing: Metadata describing the genotype VCF dataset are inserted into the database.\n\nThrows\n\nErrorException: If the VCF file does not exist.\nErrorException: If the VCF file path is not absolute.\nErrorException: If the specified reference genome is not registered in the database.\nErrorException: If the file does not appear to be a valid VCF file.\nAny database exception raised during insertion.\n\nWarnings\n\nA warning is emitted when a record for the supplied VCF file already exists in the database.\n\nNotes\n\nThe reference genome must be registered beforehand using upload_reference_genome!.\nReference genome lookup is performed using the stored file_path field.\nBoth plain-text and gzip-compressed VCF files are supported.\nVCF validation is based on detection of the mandatory #CHROM header line.\nThe resolved reference genome identifier is stored in the genotype_vcfs table.\nExisting records are preserved through the use of ON CONFLICT DO NOTHING.\nThe VCF file itself is not stored in the database; only its metadata, file path, and reference genome association are recorded.\n\nExamples\n\njulia> fname_reference_genome = string(\"simulated_reference_genome-\", Dates.now(),\".fa\");\n\njulia> fname_genomes_vcf = string(\"simulated_genotype_vcf-\", Dates.now(),\".vcf\");\n\njulia> simulate_genomes(fname_reference_genome=fname_reference_genome, fname_genomes_vcf=fname_genomes_vcf);\n\njulia> conn = dbconnect(); \n\njulia> try isnothing(upload_genotype_vcf!(conn, fname=abspath(fname_genomes_vcf), name=fname_genomes_vcf, notes=\"simulated\", fname_reference_genome=abspath(fname_reference_genome))); catch; false; end\nfalse\n\njulia> upload_reference_genome!(conn, fname=abspath(fname_reference_genome), name=fname_reference_genome, notes=\"simulated\");\n\njulia> try isnothing(upload_genotype_vcf!(conn, fname=abspath(fname_genomes_vcf), name=fname_genomes_vcf, notes=\"simulated\", fname_reference_genome=abspath(fname_reference_genome))); catch; false; end\ntrue\n\njulia> query_table(conn, filters=[Filter(conn, table=\"genotype_vcfs\", field=\"name\", filter_in=[fname_genomes_vcf])]) |> nrow == 1\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_genotype_vcf!"},{"category":"method","location":"#GenomicBreedingDB.upload_phenomes!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_phenomes!(\n    conn::LibPQ.Connection;\n    fname::String,\n    name::String,\n    notes::String,\n)::Nothing\n\nUpload a Phenomes dataset to the database and register its metadata.\n\nThe function validates that the supplied file exists and appears to be a JLD2 file containing a Phenomes object, verifies that the file path is absolute, and then registers the dataset in the phenomes table.\n\nFile validation is delegated to check(Phenomes; fname=...), which performs a lightweight inspection of the file to ensure it appears to contain a valid Phenomes object. Existing records are preserved using an ON CONFLICT DO NOTHING clause.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Absolute path to the JLD2 file containing a Phenomes object.\nname::String: Unique name used to identify the phenomic dataset.\nnotes::String: Descriptive notes associated with the phenomic dataset.\n\nReturns\n\nNothing: Metadata describing the phenomic dataset are inserted into the database.\n\nThrows\n\nErrorException: If the supplied file does not exist.\nErrorException: If the file does not appear to contain a valid Phenomes object.\nErrorException: If the file path is not absolute.\nAny database exception raised during insertion.\n\nWarnings\n\nA warning is emitted when a matching phenomes record already exists and the insert operation is ignored.\n\nNotes\n\nFile validation is delegated to check(Phenomes; fname=...).\nOnly absolute file paths are accepted.\nRecords are inserted into the phenomes table using the supplied name, file path, and notes.\nExisting records are preserved through the use of ON CONFLICT DO NOTHING.\nThe JLD2 file itself is not stored in the database; only its metadata and file location are recorded.\nThis function is intended for registering previously generated phenomic datasets rather than creating new ones.\n\nExamples\n\njulia> fname_phenomes_jld2 = string(\"simulated_phenotype_jld2-\", Dates.now(),\".jld2\");\n\njulia> genomes = simulate_genomes(); phenomes = simulate_trials(genomes) |> x -> simulate_phenomes(x, fname_phenomes_jld2=fname_phenomes_jld2);\n\njulia> conn = dbconnect(); \n\njulia> upload_phenomes!(conn, fname=abspath(fname_phenomes_jld2), name=fname_phenomes_jld2, notes=\"simulated\");\n\njulia> query_table(conn, filters=[Filter(conn, table=\"phenomes\", field=\"name\", filter_in=[fname_phenomes_jld2])]) |> nrow == 1\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_phenomes!"},{"category":"method","location":"#GenomicBreedingDB.upload_reference_genome!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_reference_genome!(\n    conn::LibPQ.Connection;\n    fname::String,\n    name::String,\n    notes::String,\n)::Nothing\n\nUpload a reference genome file to the database and register its metadata.\n\nThe function validates that the supplied file path is absolute and that the file appears to be a valid reference genome FASTA file. Following validation, the reference genome metadata are inserted into the reference_genomes table unless the file has already been registered.\n\nBefore insertion, the database is queried to determine whether the supplied file path already exists in the reference_genomes table. If exactly one matching record is found, a warning containing the existing record information is emitted and the function exits without modifying the database. An additional integrity check ensures that multiple records cannot reference the same file path.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Absolute path to the reference genome FASTA file.\nname::String: Unique name used to identify the reference genome.\nnotes::String: Descriptive notes associated with the reference genome.\n\nReturns\n\nNothing: Reference genome metadata are inserted into the database, or no action is taken if the file has already been registered.\n\nThrows\n\nErrorException: If the file path is not absolute.\nErrorException: If the reference genome file does not exist.\nErrorException: If the file does not appear to be a valid FASTA file.\nErrorException: If multiple database records reference the same file path, indicating database corruption or an inconsistent database state.\nAny database exception raised during insertion.\n\nWarnings\n\nA warning is emitted and the function returns immediately when the supplied file path has already been registered in the database.\nA warning is emitted when a reference genome with the same name already exists and the insert operation is ignored by ON CONFLICT (name) DO NOTHING.\n\nNotes\n\nOnly absolute file paths are accepted.\nReference genome validation is delegated to check_reference_genome(fname).\nExisting registrations are identified using the stored file_path field.\nDuplicate file registrations are not permitted.\nMetadata are inserted into the reference_genomes table using the supplied name, file path, and notes.\nExisting records with the same name are preserved through the use of ON CONFLICT (name) DO NOTHING.\nThe reference genome file itself is not stored in the database; only its metadata and file location are recorded.\nIf an existing record is found for the supplied file path, information about that record is reported to assist with provenance tracking and debugging.\n\nExamples\n\njulia> fname_reference_genome = string(\"simulated_reference_genome-\", Dates.now(),\".fa\");\n\njulia> simulate_genomes(fname_reference_genome=fname_reference_genome);\n\njulia> conn = dbconnect();\n\njulia> upload_reference_genome!(conn, fname=abspath(fname_reference_genome), name=fname_reference_genome, notes=\"simulated\");\n\njulia> query_table(conn, filters=[Filter(conn, table=\"reference_genomes\", field=\"name\", filter_in=[fname_reference_genome])]) |> nrow == 1\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_reference_genome!"},{"category":"method","location":"#GenomicBreedingDB.upload_trial_data!-Tuple{LibPQ.Connection}","page":"Home","text":"upload_trial_data!(\n    conn::LibPQ.Connection;\n    fname::String,\n    missing_strings::Vector{String}=[\n        \"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", \"\"\n    ],\n    species::Union{Nothing,String}=nothing,\n    experiment::Union{Nothing,String}=nothing,\n    treatment::Union{Nothing,String}=nothing,\n    entry_type::Union{Nothing,String}=nothing,\n    population_type::Union{Nothing,String}=nothing,\n    relationship_type::Union{Nothing,String}=nothing,\n    measurement_dates::Union{Nothing,Dict{String,String}}=nothing,\n    verbose::Bool=false,\n)::Nothing\n\nUpload trial data from a file into the database and populate all associated reference and phenotype tables.\n\nThe function loads and validates trial data, standardises layout information, augments missing metadata fields, and inserts or updates all required database records. This includes layouts, species, experiments, treatments, sites, measurements, entries, populations, entry relationships, traits, and phenotype observations.\n\nMeasurement dates may be provided explicitly or inferred from measurement names. Database reference tables are populated automatically where required, and associated metadata fields are updated using name-based lookups.\n\nArguments\n\nconn::LibPQ.Connection: Active PostgreSQL database connection.\nfname::String: Path to the trial data file.\nmissing_strings::Vector{String}=[\"missing\", \"NA\", \"na\", \"N/A\", \"n/a\", \"\"]: Strings that should be interpreted as missing values when reading the file.\nspecies::Union{Nothing,String}=nothing: Species name to assign when a species column is not present in the input data.\nexperiment::Union{Nothing,String}=nothing: Experiment name to assign when an experiments column is not present.\ntreatment::Union{Nothing,String}=nothing: Treatment name to assign when a treatments column is not present.\nentry_type::Union{Nothing,String}=nothing: Entry type assigned to entries when an entry_types column is not present.\npopulation_type::Union{Nothing,String}=nothing: Entry type assigned to populations when a population_types column is not present.\nrelationship_type::Union{Nothing,String}=nothing: Relationship type assigned when a relationship_types column is not present.\nmeasurement_dates::Union{Nothing,Dict{String,String}}=nothing: Optional mapping between measurement identifiers and dates.\nverbose::Bool=false: If true, display progress information and summary messages during processing.\n\nReturns\n\nNothing: Trial information is inserted into the database.\n\nThrows\n\nErrorException: If entry_type is not one of the supported values.\nErrorException: If population_type is not one of the supported values.\nErrorException: If relationship_type is not one of the supported values.\nErrorException: If the trial file cannot be loaded or fails validation.\nAny database exception raised during the import process.\n\nNotes\n\nSupported entry and population types are: cultivar, population, individual, and family.\nSupported relationship types are: member_of, clone_of, parent_is, maternal_parent_is, and paternal_parent_is.\nTrial data are loaded using load_trial_df and validated using validate_trials.\nLayout information is standardised using parse_layouts!.\nMissing metadata columns may be added automatically using add_col!.\nMeasurement dates are validated or generated using add_measurement_dates!.\nReference tables are populated using insert_names!.\nExisting measurement and entry metadata are updated using update_table_field_by_name!.\nEntry-to-population relationships are inserted using insert_entry_relationships!.\nTrait columns are detected automatically using extract_traits.\nPhenotype observations are inserted using insert_phenotype_data!.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> conn = dbconnect();\n\njulia> try upload_trial_data!(conn, fname=\"simulated_trials.tsv\"); catch; false; end\nfalse\n\njulia> upload_trial_data!(conn, fname=\"simulated_trials.tsv\", species=\"Acacia neglecta\", experiment=\"some-exp\", treatment=\"some_trt\", entry_type=\"family\", population_type=\"population\", relationship_type=\"member_of\");\n\njulia> execute(conn, \"SELECT * FROM species\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM entries\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM entry_relationships\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM experiments\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM sites\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM treatments\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM layouts\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM measurements\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM traits\") |> DataFrame |> nrow > 0\ntrue\n\njulia> execute(conn, \"SELECT * FROM phenotype_data\") |> DataFrame |> nrow > 0\ntrue\n\njulia> close(conn);\n\n\n\n\n\n","title":"GenomicBreedingDB.upload_trial_data!"},{"category":"method","location":"#GenomicBreedingDB.validate_data_table-Tuple{DataFrames.DataFrame}","page":"Home","text":"validate_data_table(\n    df::DataFrame,\n)::Nothing\n\nValidate that a DataFrame conforms to the expected structure of a phenotype or environmental data table.\n\nThe function checks that the DataFrame contains the required fields needed for long-format observational data. Field names ending in _id are normalised by removing the suffix before validation, allowing both identifier-based and name-based representations of the data.\n\nThe dataset must contain all core experimental design fields together with a value field and exactly one of either trait or environmental_variable.\n\nArguments\n\ndf::DataFrame: Data table to validate.\n\nReturns\n\nNothing: Returned when the DataFrame satisfies the expected structure.\n\nThrows\n\nErrorException: If required fields are missing from the DataFrame.\n\nNotes\n\nIdentifier fields ending in _id are treated as equivalent to their corresponding name-based fields.\nRequired fields include: experiment, site, treatment, layout, measurement, entry, and value.\nExactly one of trait or environmental_variable may be omitted.\nThe function supports both phenotype and environmental data tables.\nValidation is limited to field presence and does not verify data types or field contents.\nThe function performs validation only and does not modify the input DataFrame.\n\nExamples\n\njulia> df_okay_phe = DataFrame(experiment=[], site=[], treatment=[], layout=[], measurement=[], entry=[], trait=[], value=[]);\n\njulia> df_okay_env = DataFrame(experiment=[], site=[], treatment=[], layout=[], measurement=[], entry=[], environmental_variable=[], value=[]);\n\njulia> df_nope = DataFrame(experiment=[], site=[]);\n\njulia> try isnothing(validate_data_table(df_okay_phe)); catch; false; end\ntrue\n\njulia> try isnothing(validate_data_table(df_okay_env)); catch; false; end\ntrue\n\njulia> try isnothing(validate_data_table(df_nope)); catch; false; end\nfalse\n\n\n\n\n\n","title":"GenomicBreedingDB.validate_data_table"},{"category":"method","location":"#GenomicBreedingDB.validate_date-Tuple{String}","page":"Home","text":"validate_date(\n    date::String,\n)::Nothing\n\nValidate that a string represents a valid date in yyyy-mm-dd format.\n\nThe function verifies that the supplied string conforms to the expected date format and that each component can be parsed as an integer. It then attempts to construct a Date object to confirm that the date is valid according to the Gregorian calendar.\n\nBoth formatting errors and invalid calendar dates result in an exception being raised.\n\nArguments\n\ndate::String: Date string to validate.\n\nReturns\n\nNothing: Returned when the supplied date is valid.\n\nThrows\n\nErrorException: If the date does not conform to the yyyy-mm-dd format.\nErrorException: If any date component is not an integer.\nErrorException: If the date is not a valid calendar date.\n\nNotes\n\nDates must follow the format yyyy-mm-dd.\nThe year component must contain four digits.\nMonth and day components must contain one or two digits.\nValidation includes both format checking and calendar validation.\nInternally, the date is validated using Date(date, dateformat\"yyyy-mm-dd\").\nThe function performs validation only and does not return a Date object.\n\nExamples\n\njulia> validate_date(\"2026-07-08\") |> x -> isnothing(x)\ntrue\n\njulia> validate_date(\"2026-7-8\") |> x -> isnothing(x)\ntrue\n\njulia> try validate_date(\"2026/07/08\"); catch; false; end\nfalse\n\njulia> try validate_date(\"2026|07|08\"); catch; false; end\nfalse\n\njulia> try validate_date(\"2026/JUL/08\"); catch; false; end\nfalse\n\njulia> try validate_date(\"2026-July-8\"); catch; false; end\nfalse\n\njulia> try validate_date(\"2026-02-30\"); catch; false; end\nfalse\n\n\n\n\n\n\n","title":"GenomicBreedingDB.validate_date"},{"category":"method","location":"#GenomicBreedingDB.validate_filters-Tuple{Vector{Filter}}","page":"Home","text":"validate_filters(\n    filters::Vector{Filter},\n)::Nothing\n\nValidate that a collection of filters references exactly one database table.\n\nThe function examines all supplied Filter objects and verifies that they target the same table. This constraint is required for operations that construct a single SQL query from multiple filters, such as querying or updating records.\n\nIf filters referencing multiple tables are detected, an error is raised describing the offending filter definitions.\n\nArguments\n\nfilters::Vector{Filter}: Collection of filters to validate.\n\nReturns\n\nNothing: Returned when all filters reference the same table.\n\nThrows\n\nErrorException: If the filters reference more than one database table.\n\nNotes\n\nThe function does not validate individual filter contents.\nValidation is limited to ensuring table consistency across all filters.\nAn empty filter collection is not explicitly checked and may require validation elsewhere in the workflow.\nThis function is commonly used before constructing SQL queries from multiple filter conditions.\nThe function performs validation only and does not modify the supplied filters.\n\nExamples\n\njulia> conn = dbconnect();\n\njulia> filters = Filter[];\n\njulia> push!(filters, Filter(conn, table=\"phenotype_data\", field=\"trait\", filter_like=\"trait_\"));\n\njulia> push!(filters, Filter(conn, table=\"phenotype_data\", field=\"value\", filter_between=(10, 20)));\n\njulia> push!(filters, Filter(conn, table=\"environment_data\", field=\"value\", filter_less_than=50));\n\njulia> try isnothing(validate_filters(filters)); catch; false; end\nfalse\n\njulia> pop!(filters);\n\njulia> try isnothing(validate_filters(filters)); catch; false; end\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.validate_filters"},{"category":"method","location":"#GenomicBreedingDB.validate_trials-Tuple{DataFrames.DataFrame}","page":"Home","text":"validate_trials(\n    df::DataFrame,\n)::Nothing\n\nValidate that a DataFrame conforms to the expected structure and content of a trial dataset.\n\nThe function verifies that all required trial columns are present, validates the contents of string-valued columns against the project's naming conventions, and checks that only expected fields are stored using numeric data types.\n\nRequired columns are determined from the Trials structure definition, excluding phenotype and trait matrices. String columns are validated using check_illegal_strings, whilst numeric columns are checked against a predefined list of permitted numeric fields.\n\nArguments\n\ndf::DataFrame: Trial dataset to validate.\n\nReturns\n\nNothing: Returned when all validation checks pass successfully.\n\nThrows\n\nErrorException: If one or more required columns are missing.\nErrorException: If a string column contains illegal characters or strings.\nErrorException: If unexpected numeric columns are detected.\n\nNotes\n\nRequired columns are derived from the fields of the Trials structure.\nFields containing phenotypes or traits are excluded from the required-column check.\nString-valued columns are validated using check_illegal_strings.\nNumeric columns are expected only for: years, measurements, replications, blocks, rows, and cols.\nAny additional numeric columns are treated as potential data-formatting errors and will cause validation to fail.\nThe function performs validation only and does not modify the input DataFrame.\n\nExamples\n\njulia> simulate_genomes() |> simulate_trials;\n\njulia> df = load_trial_df(\"simulated_trials.tsv\");\n\njulia> isnothing(validate_trials(df))\ntrue\n\n\n\n\n\n","title":"GenomicBreedingDB.validate_trials"}]
}
